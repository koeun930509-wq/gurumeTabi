# CLAUDE.md

이 파일은 이 저장소에서 작업할 때 Claude Code(claude.ai/code)에게 제공하는 가이드입니다.

## 프로젝트

Gurume Tabi (구루메 타비) — 일본 여행 맛집 리뷰 플랫폼. 맛집 데이터는 이제 **Google Places API로 수집한 실데이터**를 씁니다(`src/lib/restaurants.js`가 Supabase `restaurants`/`reviews_cache` 테이블을 조회 — `src/data/mockRestaurants.js`는 더 이상 어디서도 import되지 않는 죽은 파일로 남아있음, 참고용으로만 유지). 인증도 Supabase Auth로 실제 연동되어 있습니다(`AuthContext.login(email, password)`가 `src/lib/supabaseClient.js`를 통해 실제 로그인 검증을 수행하고, 회원가입·비밀번호 변경도 실제로 동작 — 아래 인증 관련 항목 참고). 스크랩·검색 기록은 아직 localStorage 기반. 전체 제품 기획은 `../PRD.md` 참고.

**데이터 수집 파이프라인**: Flask 백엔드 대신 Supabase Edge Function으로 구현됨(PRD 9절 원안과 다름 — 아래 "실데이터 수집 파이프라인" 절 참고).

## 명령어

```bash
npm run dev      # vite 개발 서버
npm run build    # vite 빌드
npm run lint      # oxlint (ESLint 아님 — 설정은 .oxlintrc.json에 있음)
npm run preview  # 프로덕션 빌드 프리뷰
npm test         # vitest run (Vitest + React Testing Library, jsdom 환경)
```

테스트 파일은 테스트 대상 코드 옆에 위치(`*.test.jsx`). Vitest 설정은 `vite.config.js` 안에 인라인(`test: {...}`)으로 되어 있고, 셋업 파일은 `src/setupTests.js`.

## 실데이터 수집 파이프라인 (Supabase Edge Functions)

맛집 데이터는 Flask가 아니라 **Supabase Edge Function**이 Google Places API를 직접 호출해 채운다. 전부 Supabase 프로젝트 `aeipftjmlikejemppxii`에 배포되어 있고, MCP로 소스를 조회·재배포할 수 있음.

- **`sync-restaurants`** — 핵심 수집 함수. `{ region, category, query, maxResults }`를 받아 Google Places Text Search(New)를 호출하고 `restaurants`(+`reviews_cache`)에 upsert한다.
  - **한글 지명 검색 버그**: Google Places Text Search가 순수 한글 지명 쿼리를 인식 못 해 0건이 나오는 이력이 있었음(예: "오사카" 단독). 그래서 `REGION_EN` 매핑으로 지역명을 영어로 치환해서 보냄(`languageCode: "ko"`와 조합하면 한글 번역 상호명도 우선 받아짐).
  - **비-식당 결과 자동 제외**: `places.types`에 `shopping_mall`/`department_store`/`tourist_attraction` 등이 섞여 있으면 건물/시설 자체라 제외(`isNonRestaurantPlace`) — 캐널시티 하카타(쇼핑몰)가 "후쿠오카 타코야키"로 잘못 들어온 사례가 있었음.
  - **국가 필터**: `addressComponents`의 country가 `JP`가 아니면 제외(`isOutsideJapan`) — "도쿄라멘 지족점"(대전), "무쿄쿠 인천공항점"처럼 한국 내 일본풍 상호명이 검색어와 이름이 일치해 섞여 들어온 이력이 있음(2026-08, 총 60건 발견·삭제됨). **이 필터는 2026-08-15 이후 수집분에만 적용**되어 있으니, 그 이전 수집된 지역에 유사 오염이 남아있을 수 있다는 걸 감안할 것.
  - **번역**: 가게명·리뷰 본문이 한글이 아니면 Cloud Translation API로 배치 번역(`translateToKorean`) — 이미 한글이면 API 호출 생략.
  - **리뷰**: `places.reviews`(가게당 최대 5개)를 `reviews_cache`에 저장. `is_ad_filtered`는 Google 리뷰는 전부 `false`(실제 방문자 리뷰이므로 애초에 광고 판별 대상이 아님 — 네이버 협찬 판별용으로 설계된 필드를 그대로 씀).
  - **사진**: 가게당 최대 5장(`MAX_PHOTOS`)을 `image_urls`(배열)에 저장, `image_url`은 그 첫 장.
  - **재수집 시 중복 방지**: `google_place_id`가 유니크 제약이라 upsert로 자동 갱신됨. 리뷰는 매번 해당 가게의 기존 `source='google'` 행을 지우고 다시 씀.
- **`sync-naver-reviews`** — 네이버 블로그 검색 API(NAVER API HUB)로 가게별 협찬 필터링 리뷰를 채우는 함수. `{ restaurantId }`를 받아 `region + name`으로 블로그를 검색하고 `reviews_cache`에 `source='naver'`로 upsert한다. 2026-08-18에 전체 1,748개 가게에 대해 실행 완료(1,470곳에 리뷰 붙음, 총 6,598건).
  - **API가 developers.naver.com에서 NAVER API HUB(NCP)로 완전히 이전됨**: `developers.naver.com/apps/#/register`의 "사용 API" 드롭다운에는 더 이상 "검색"이 없음(로그인/인증서/전자문서/카페/캘린더/캡차만 남음) — NAVER API HUB 콘솔에서 별도로 Application을 등록해 발급받아야 함.
  - **인증 헤더**: 예전 `X-Naver-Client-Id`/`X-Naver-Client-Secret` 방식이 아니라 `X-NCP-APIGW-API-KEY-ID`/`X-NCP-APIGW-API-KEY`를 씀. Secret 이름은 `NAVER_CLIENT_ID`/`NAVER_CLIENT_SECRET`.
  - **Base URL 주의**: `https://naverapihub.apigw.ntruss.com`이 맞음(`naveropenapi.apigw.ntruss.com`으로 착각해서 첫 배포가 "URL not found" 502로 실패한 이력 있음).
  - **NCP IAM 키(`ncp_iam_...`로 시작하는 Access Key/Secret Key)와 혼동 주의**: 이건 계정 전체 레벨 인증키로 Search API와 무관함. Search API 전용 키는 NAVER API HUB → Application 메뉴에서 별도 발급.
  - **요금**: 검색 API는 유료 구간 없이 무료(0~775,000건), 일 최대 25,000건 호출 제한만 있음.
  - **협찬 필터링**: 검색 스니펫에서 "협찬"/"제공받아"/"체험단" 등 키워드가 있으면 `is_ad_filtered=true`로 표시. 스니펫만으로는 완벽한 판별이 불가능(본문 전체 크롤링은 범위 밖).
  - **맛 표현 추출(`taste_snippet`)**: 네이버 블로그 검색 스니펫에서 "맛있"/"쫄깃"/"바삭"/"가성비" 등 25개 맛 관련 키워드가 포함된 구절(마침표/줄바꿈/해시태그/말줄임표 기준으로 자른 clause 단위)만 추출해 별도 컬럼에 저장. LLM 없이 순수 키워드 매칭 방식(추가 API 비용 없음). 2026-08-18에 `taste_snippet` 컬럼 추가 후 전체 백필 진행.
  - **원문 링크(`link`)**: 네이버 블로그 검색 API 응답의 `item.link`(블로그 글 URL)를 그대로 저장 — **API가 개별 글 링크를 안 준다는 이전 기록은 오류였음**(구글 리뷰와 혼동해서 잘못 기록됨). 이 필드 덕분에 `ReviewCard.jsx`에서 네이버 리뷰 카드도 클릭 시 원문 블로그로 새 탭 이동 가능(구글 리뷰와 동일하게 `<a target="_blank">`). 2026-08-18 이전 수집분은 `link`가 비어있어 그 리뷰들은 자동으로 클릭 비활성화 폴백됨 — 전체 재수집 전까지는 일부 카드가 클릭 안 되는 게 정상.
- **`flag-rude-reviews`** — `reviews_cache`를 스캔해서 별점 2점 이하 + 불친절 관련 키워드(불친절/무례/짜증/rude 등)가 있는 리뷰가 하나라도 있으면 그 가게의 `restaurants.has_rude_review`를 `true`로 갱신. 리뷰가 새로 쌓일 때마다 재실행 필요.
- **`fill-walk-minutes`** — Places Nearby Search로 가게 좌표 기준 가장 가까운 `train_station`을 찾아 직선거리를 도보 분으로 환산(`walk_minutes`)하고 역 이름(`nearest_station`)도 저장. **오키나와는 `SKIP_REGIONS`로 항상 건너뜀**(본토와 철도로 안 이어져 있어 렌터카 이동이 대부분이라 "역 도보거리" 자체가 무의미하다고 판단함).
- **`translate-restaurant-names`** — 기존에 저장된 레코드 중 한글이 아닌 이름만 다시 스캔해서 보정하는 백필 함수(신규 수집 시 자동 번역되지만, 과거 데이터 보정용으로 별도 존재).
- **`enrich-hotpepper`** — 리쿠르트 Hot Pepper Gourmet API(グルメサーチAPI)로 기존 Google Places 데이터를 보강하는 함수. `{ restaurantId }`를 받아 그 가게의 위경도로 Hot Pepper를 검색하고, **가게명이 아니라 거리 기반으로 매칭**함(반경 80m 이내 최근접 후보만 인정) — DB 가게명이 영어/한글/일본어가 섞여 있어(수집 시 자동 번역됨) Hot Pepper가 주는 일본어 원문 이름과 문자 유사도 비교가 사실상 불가능했기 때문(테스트 시 정확히 같은 가게인데도 유사도 0으로 나옴). 매칭되면 `business_hours`/`regular_holiday`/`budget_dinner`/`budget_lunch`/`hotpepper_url`을 채우고, `accepts_card`는 Hot Pepper가 "可"를 명시할 때만 `true`로 덮어씀(약한 신호로 기존 Google 값을 잘못 덮어쓰지 않도록). Secret 이름은 `HOTPEPPER_API_KEY`. 2026-08-18에 전체 1,970개 가게에 실행 완료 — 1,589곳 매칭(80.7%), 나머지는 반경 안에 Hot Pepper 미등록 매장이거나 진짜로 없는 경우.
  - **번역**(2026-08-18): `business_hours`/`regular_holiday`/`budget_dinner`/`budget_lunch`는 Hot Pepper가 주는 자유 텍스트 일본어 원문(예: "月～日、祝日、祝前日: 12:00～翌5:00", "年中無休", "お通し料439円（税込）")을 `sync-restaurants`와 동일한 Cloud Translation API(`GOOGLE_PLACES_API_KEY` secret 재사용)로 한글 번역해서 저장함(예: "월~일, 공휴일, 공휴일 전날: 12:00~다음 5:00", "연중무휴", "통행료 439엔(부가세 포함)") — 처음엔 원문을 그대로 저장했다가 사용자 피드백으로 번역 추가, 이미 보강했던 1,589곳 전체를 재실행해 번역본으로 갱신함. 자동번역이라 "お通し料"(자릿세/기본안주 요금 관용구) 같은 표현은 다소 어색하게 나올 수 있음(예: "통행료") — 완전히 틀린 건 아니라 별도 사전 매핑 없이 감수하기로 함.
  - **UI 노출**(2026-08-18): `RestaurantDetailPage.jsx` 사이드바의 주소·전화번호 블록 바로 아래에 영업시간(`IconClock`, 정휴일이 있고 "없음"이 아닐 때만 옆에 병기 — 번역 전엔 "なし"로 비교했었으니 이 문자열이 다시 바뀌면 같이 고칠 것)·예산대(`IconWallet`, 디너/런치 중 있는 것만)·핫페퍼 링크(`IconExternalLink`, "핫페퍼 그루메에서 보기")를 조건부로 추가함 — 매칭 안 된 가게(약 20%)는 이 블록 전체가 그냥 안 보이고 주소·전화번호만 남음.

**수집 범위**: 14개 지역(대도시 6곳 도쿄/오사카/후쿠오카/나고야/삿포로/오키나와는 `maxResults: 20`, 소도시 8곳 다카마쓰/마쓰야마/오카야마/히로시마/시즈오카/요나고/기타큐슈/나가사키는 `maxResults: 15`) × 6개 카테고리(스시/라멘/우동/오코노미야키/돈카츠/타코야키) = 84개 조합. 이 84개 조합은 매번 Node.js 스크립트(`.mjs`)로 순회 호출해서 수집함(스크립트 파일은 저장 안 해두고 대화 중 임시로 작성 — curl은 Windows 콘솔 코드페이지(CP949) 때문에 한글 쿼리가 깨져서 반드시 Node `fetch` 스크립트를 쓸 것) — 재수집이 필요하면 이 조합 리스트를 그대로 재사용할 것. 네이버 리뷰 재수집은 `restaurants` 테이블 전체 id를 페이지네이션 조회해서 `sync-naver-reviews`를 순회 호출하는 방식(가게당 딜레이를 둬서 API 초당 호출 제한 대응).

**디저트 카테고리 추가 수집(2026-08-18)**: `FOOD_TYPES`(`src/utils/searchTerms.js`)에는 원래부터 "디저트"·"카페"가 검색 자동완성/필터 항목으로 들어있었지만, 실제 `restaurants` 테이블에는 그동안 6개 카테고리(스시/라멘/돈카츠/우동/오코노미야키/타코야키)만 수집되어 있어서 "디저트"로 검색하거나 필터를 걸면 결과가 항상 0건이었음 — "근처 디저트 맛집" 기능 추가를 계기로 `sync-restaurants`를 14개 지역 × `category: "디저트", query: "디저트"`로 순회 호출해 224곳을 신규 수집(기타큐슈만 1곳으로 적음, Google Places 검색 결과 자체가 적었던 것으로 해당 지역 디저트 카페가 드문 편). 이어서 `sync-naver-reviews`도 이 224곳에 대해 순회 호출해 네이버 리뷰까지 채움. "카페"는 아직 미수집 상태로 남아있음(필요 시 같은 방식으로 `category: "카페"` 조합만 추가하면 됨).

**재수집 주기**: 분기(3개월) 1회로 재수집을 권장하기로 사용자와 합의함(2026-08-19, 기존 월 1회에서 변경 — 84개 지역×카테고리 + 디저트 14개 조합 전체를 한 번 재수집할 때마다 Google Places Text Search + Cloud Translation API 비용이 약 9~10만 원 발생하는 것을 확인, 무료 크레딧 소진 속도를 늦추기 위해 주기를 늘림). pg_cron 등 완전 자동화는 하지 않기로 함(매번 사용자 확인 후 진행하는 방식 유지).

## 스타일링 — Tailwind v4, config 파일 없음

테마는 `src/index.css`의 `@theme{}` 블록 안에 있습니다 — **`tailwind.config.js`는 없습니다**. 알아둘 커스텀 토큰:

- 브랜드 컬러: `--color-brand-navy` (#6D28D9), `--color-brand-coral` (#A855F7), `--color-brand-peach` (#EDE4FB), 각각 `-dark` 변형 있음. 여기에 `--color-status-open/closed/soldout` 추가. 이건 Tailwind 기본값이 아니므로 `bg-brand-coral`, `text-brand-navy` 등으로 사용.
- 폰트 크기 스케일은 Tailwind 기본값이 아니라 **오버라이드**되어 있음: `text-xs`부터 `text-2xl`까지 한 단계씩 위로 밀려 있음(예: `text-base` = 1.125rem, 1rem 아님). 픽셀 값을 추론할 때 Tailwind 기본 타입 스케일을 가정하지 말고, 먼저 `src/index.css`의 `@theme` 블록을 확인할 것.
- Body에는 전역 그라디언트 배경 + `background-attachment: fixed`가 Tailwind 유틸리티가 아니라 `index.css`에 직접 설정되어 있음.
- 대형 미디어(영상/풀블리드 히어로 이미지, 로고)는 `public/`에 있고 `src/assets`에서 import하는 대신 절대경로(`/sushi.mp4`, `/logo.png`, `/loginBg.png`)로 참조함 — Vite가 이걸 번들링/해싱하지 않게 하려는 의도적 선택. 상대적으로 작은 장식용 이미지(예: `src/assets/hero.png`)는 여전히 일반적인 `import x from '../assets/...'` 패턴을 씀. 어느 페이지가 어떤 방식을 쓰는지 확인 없이 이 불일치를 한쪽으로 "통일"하려 하지 말 것.

## 코드 스타일

- 싱글 쿼트, 세미콜론 없음, 함수형 컴포넌트만 사용
- 상태 관리: React Context만 사용(`src/context/AuthContext.jsx`), Redux/Zustand 없음
- 커스텀 훅 패턴: `useAuth()`가 `useContext(AuthContext)`를 감싸며, Provider와 같은 파일에 정의됨. `AuthContext` 자체도 export됨(테스트에서 `AuthProvider`의 localStorage 로직을 거치지 않고 mock `AuthContext.Provider`로 컴포넌트를 감쌀 수 있도록) — 이로 인해 oxlint의 `only-export-components` 경고가 뜨는데, 여기서는 의도된 것이고 문제없음
- Tailwind 클래스는 JSX에 직접 인라인; `navLinkClass(isActive)` 같은 헬퍼 함수가 템플릿 문자열 클래스 조합을 반환 — `clsx`/`cva` 없음

## 라우팅 & 인증

- 라우트는 `src/App.jsx`에서 `<Routes>/<Route>`로 선언
- `ProtectedRoute`(`src/components/ProtectedRoute.jsx`)가 로그인이 필요한 라우트를 감쌈 — `state={{from: location}}`과 함께 `/login`으로 리다이렉트하고, `LoginPage`는 로그인 성공 후 그 위치로 다시 돌려보냄. `/mypage`와 `/scrap` 둘 다 감싸져 있음
- `/signup`은 Supabase Auth로 실제 회원가입을 처리함 — 이 프로젝트는 이메일 확인(Confirm email)이 꺼져 있어 가입 즉시 로그인 상태가 됨(별도 메일함 확인 불필요)

## 주의할 점 (Gotchas)

- 브랜드명은 이제 어디서나 "Gurume Tabi"임. 예전 중복 뼈대(과거 `../jtaste-pass`에 있던 초안 "J-Taste Pass" 브랜딩)는 삭제됨 — 이 `gurumeTabi` 폴더가 유일한 실제 버전임.
- **맛집 데이터는 이제 실데이터임**: `src/lib/restaurants.js`(`fetchRestaurants`/`fetchRestaurantById`/`fetchBackupPlan`)가 Supabase `restaurants`/`reviews_cache` 테이블을 실제로 조회함(`@supabase/supabase-js` 사용) — `src/data/mockRestaurants.js`는 더 이상 어디서도 import 안 되는 죽은 파일. 리뷰(`reviews`), 사진(`image`/`images`), 도보거리(`walkMinutes`/`nearestStation`) 등도 전부 이 실데이터 기준. Flask 백엔드는 여전히 존재하지 않고, 대신 Supabase Edge Function이 그 역할을 대신함(위 "실데이터 수집 파이프라인" 절 참고).
- **Google Places가 못 채우는 필드는 DB에 null로 남아있고, 프론트에서 기본값으로 안전 처리됨**: `localRatio`(현지인 비율)·`hasRudeReview`·`acceptsCard`·`walkMinutes`·`acceptsReservation`는 `toViewModel`(`src/lib/restaurants.js`)에서 `?? 기본값`으로 폴백. `hasRudeReview`만 `flag-rude-reviews` Edge Function으로 실제 리뷰 기반 채워짐 — 나머지는 대부분 아직 null/false 상태로, `accepts_card`/`accepts_reservation`은 Places API의 `paymentOptions.acceptsCreditCards`/`reservable`로 채워지지만(2026-08 이후 수집분), `walkMinutes`/`nearestStation`은 `fill-walk-minutes`를 별도로 돌려야 채워짐.
- 관리자용 데이터 정리/재수집 작업(Edge Function 재배포, 대량 upsert, 오염 데이터 삭제 등)은 전부 Claude Code가 Supabase MCP로 대화 중 직접 수행함 — 이건 앱 런타임 코드가 아니라 운영 채널이니 프론트 코드에서 이런 로직을 찾으려 하지 말 것.
- **Supabase MCP 연결됨**: 프로젝트 `aeipftjmlikejemppxii`(https://aeipftjmlikejemppxii.supabase.co)에 `claude mcp add supabase`로 연결되어 있음(full access, `claude mcp list`에서 `supabase` 확인 가능) — `profiles`/`restaurants`/`scraps`/`reviews_cache`/`search_history` 등 실제 테이블은 PRD.md 8절 스키마 설계대로 생성 완료됨(RLS 적용). `profiles`에는 관리자 구분용 `is_admin`(boolean, 기본 false) 컬럼도 추가했고 관리자 계정 1개를 만들어 `true`로 표시해둠 — 다만 이 플래그를 실제로 검사하는 관리자 화면/로직은 아직 없음. 등록 명령은 `npx`/`-y` 대신 전역 설치한 `mcp-server-supabase` 바이너리를 직접 씀(`npm install -g @supabase/mcp-server-supabase` 후 `claude mcp add supabase -e SUPABASE_ACCESS_TOKEN=... -- mcp-server-supabase --project-ref=aeipftjmlikejemppxii`) — Windows PowerShell에서 `claude mcp add ... -- <cmd> <flags>` 형태로 실행하면 `--` 뒤의 옵션(`-y`든 `--project-ref=...`든 대시로 시작하는 건 전부)이 `claude mcp add` 자체의 옵션으로 잘못 파싱되는 버그가 있었음(Bash/git bash로 실행하면 문제없이 통과됨) — 같은 문제가 재발하면 옵션 없는 명령만 `--` 뒤에 오도록 래퍼 스크립트로 옵션을 감추거나, PowerShell 대신 Bash로 실행할 것.
- 프로젝트 루트에는 관련 없는 한국어 `.docx` 워크시트 파일들과 `drive-download-*.zip`도 있음 — 이건 강의/기획 자료이고 앱 코드가 아니며, gitignore 처리됨(`*.docx`, `drive-download-*.zip`).
- `design-system/MASTER.md` + `design-system/mockups/*.html`은 `ui-ux-pro-max` 스킬로 생성된 별도의 디자인 토큰 레퍼런스 및 standalone HTML 프로토타입임 — 실제 React 앱의 일부가 아니고 어떤 라우트에도 연결되어 있지 않음. `src/pages/`의 실제 페이지들과 혼동하지 말 것; `MASTER.md`는 `src/index.css`에 이미 존재하는 토큰들을 문서화한 것으로 취급하고, 여기서 색상을 동기화해와야 하는 새로운 source of truth로 취급하지 말 것.
- `src/pages/HomePage.jsx.bak`은 리워크 이전 `HomePage.jsx`를 수동으로 백업해둔 스냅샷임(안전망용, 어디서도 import되지 않음) — 복원/diff 요청이 있을 때가 아니면 무시할 것.
- 검색어 매칭용 상수(`FOOD_TYPES`, `REGIONS`)와 일본어 음차 표기 정규화 함수(`normalizeJapaneseTranscription`, "오코노미야키"/"오꼬노미야끼"처럼 평음·경음·격음 표기 차이를 흡수)는 `src/utils/searchTerms.js`에 있음 — `SearchResultsPage.jsx`에 중복 정의하지 말고 여기서 import. 같은 파일의 `suggestSearchTerms`(예전 이름 `suggestFoodTypes`)는 `REGIONS`+`FOOD_TYPES`를 함께 검색하므로, 입력값이 지역명과 음식명 양쪽에 매칭될 수 있음(예: "오" → "오사카"·"오코노미야키"·"오뎅" 등이 한 목록에 같이 뜸) — 음식 종류만 다시 필터링하는 별도 함수를 새로 만들지 말 것. **`REGIONS`는 이제 검색 자동완성 추천용으로만 쓰임** — 위에서 설명한 대로 `SearchResultsPage.jsx`의 사이드바 지역 필터 UI 자체가 삭제됐으므로, `REGIONS`를 필터 체크박스 목록으로 다시 쓰려 하지 말 것.
- `SearchAutocompleteInput.jsx`는 값이 있을 때 마우스 클릭뿐 아니라 키보드(↓/↑로 추천 항목 순환 하이라이트, Enter로 하이라이트된 항목 선택, 하이라이트 없이 Enter면 부모 `<form>`의 기본 submit으로 흘러가게 그대로 둠, Esc로 닫기)도 지원함 — `activeIndex` state로 하이라이트를 관리하며 `value`가 바뀌면 `-1`로 리셋됨. 이 컴포넌트를 쓰는 곳(홈·검색·헤더)마다 옆에 있는 돋보기 아이콘과 별개로, 텍스트가 있을 때 그 아이콘 왼쪽에 X 지우기 버튼을 각 페이지에서 조건부로 렌더링해둠(컴포넌트 자체에는 X 버튼이 없음) — 위치·크기는 페이지마다 옆 아이콘 크기에 비례해서 다르게 잡혀 있으니 통일하려 하지 말 것.
- `SearchAutocompleteInput`(`src/components/SearchAutocompleteInput.jsx`)은 헤더 검색창과 `/search` 페이지의 검색창 둘 다에서 쓰는 공용 컴포넌트임 — 검색 입력 UI/자동완성 동작을 고칠 때 한쪽만 수정하고 다른 쪽을 빠뜨리지 않도록 두 사용처(`Header.jsx`, `SearchResultsPage.jsx`) 모두 확인할 것.
- `AccountActions`(`src/components/AccountActions.jsx`)는 프로필 아바타(44px 원형, 클릭 시 `/mypage`)와 로그아웃 버튼(비로그인 시 로그인 아이콘 링크)을 묶은 공용 컴포넌트. `showLogout` prop(기본 `true`)으로 로그아웃 아이콘만 숨길 수 있음 — `Header.jsx`가 모바일에서 메뉴 닫힘 상태일 때 `showLogout={false}`로 아바타만 보여주고, 로그아웃은 모바일 드롭다운 메뉴 안으로 옮겨서 별도로 렌더링함(아래 헤더 항목 참고). `Header.jsx`(데스크톱 nav), `MyPage.jsx`, `ScrapPage.jsx`에서 재사용됨 — 마크업을 각 파일에 다시 베껴 넣지 말고 이 컴포넌트를 import해서 쓸 것. `MyPage.jsx`/`ScrapPage.jsx`에서는 콘텐츠 영역 위에 `absolute top-0 right-0`로 겹쳐서 배치(아래 스크롤 관련 항목 참고) — 더 이상 타이틀 옆 일반 flow가 아님.
- `Header.jsx`의 모바일(`md` 미만) 헤더: 메뉴 닫힘 상태는 로고+아바타+햄버거(28px, `w-7 h-7`)만 노출. 햄버거를 누르면 아바타가 사라지고 X로 바뀌며, `MOBILE_MENU`(검색/스크랩 맛집/마이페이지, `Sidebar.jsx`의 `MENU`와 동일한 아이콘 세트) 드롭다운이 열림 — 각 항목에 아이콘이 붙고 `active` 일치 시 전체 행이 `bg-brand-peach/40`로 하이라이트(텍스트 색만 바꾸던 이전 방식과 다름). 로그아웃(비로그인 시 로그인)은 드롭다운 맨 아래 `mt-auto` 행으로 항상 고정되고 항상 하이라이트 배경(`bg-brand-peach/30`)이 켜져 있음 — 데스크톱 nav의 로그아웃(항상 `AccountActions` 안에 있음)과는 별개 마크업이니 모바일 메뉴 항목을 고칠 때 데스크톱 쪽과 헷갈리지 말 것.
- `Sidebar.jsx`(`/mypage`, `/scrap` 데스크톱 좌측 메뉴)의 로고는 `Header.jsx`의 로고와 **완전히 동일한** 여백·크기 클래스(`px-4 sm:px-6 py-6`, `h-[43.2px] sm:h-12`)를 씀 — 두 컴포넌트가 별개 파일이라 임의로 하나만 바꾸면 홈/검색(Header) ↔ 마이페이지/스크랩(Sidebar) 이동 시 로고가 위치·크기가 달라져 화면이 흔들리게 됨. 로고 스타일을 고칠 때는 항상 두 파일을 함께 수정할 것.
- `RestaurantCard.jsx`는 삭제됨. `/scrap`(`ScrapPage.jsx`)도 `/search` 결과와 동일한 `SearchResultGridCard`를 그대로 재사용하도록 통일했음 — 카드 UI를 고칠 때 검색 결과와 스크랩 화면에 동시에 반영됨을 염두에 둘 것.
- `SearchResultGridCard.jsx`(그리드)와 `SearchResultCard.jsx`(리스트) 둘 다 `#{restaurant.region} #{restaurant.category}` 해시태그를 표시함 — 한쪽만 고치고 다른 쪽을 빠뜨리지 않을 것. 저장(⭐) 버튼도 두 컴포넌트 모두 실제 `<button>`으로 `toggleScrap`에 연결돼 있고(리스트뷰는 원래 클릭 안 되는 `<span>`이었다가 나중에 버튼으로 바뀜), `cursor-pointer` + hover 시 별 아이콘 자체에 `stroke-brand-coral` 테두리가 나타남(버튼의 원형 배경이 아니라 `IconStar`에 `group-hover:stroke-brand-coral` 클래스를 줌). `SearchResultCard.jsx`는 카드 전체가 `<Link>`라서, 저장 버튼 클릭이 상세 페이지 이동으로 새는 걸 막으려고 `preventDefault`+`stopPropagation`을 씀. `SearchResultCard.jsx`의 썸네일은 카드 상/하/좌 여백 없이 꽉 차게(`self-stretch`, 카드 높이에 맞춰 늘어남) 폭 150px 고정 — 정사각형이 아니라 카드 높이를 그대로 따라가는 세로로 긴 직사각형에 가까움. **`img`는 반드시 `absolute inset-0`로 배치할 것** — Google 실사진 중 극단적으로 세로가 긴 원본(예: 800×1422)이 섞여 있어서, `img`가 일반 흐름에 있으면 `self-stretch`인 부모가 그 이미지의 intrinsic aspect ratio에 영향받아 카드 전체가 불필요하게 길어지는 버그가 실제로 났었음(카드 텍스트 컬럼과 무관하게 이미지 높이가 카드 높이를 결정해버림).
- `SearchResultGridCard.jsx`의 썸네일에는 **사진 캐러셀**이 있음 — 가게당 사진이 여러 장(`restaurant.images`, 최대 5장)이라 PC는 hover 시 좌우 화살표(`IconChevronRight`, 왼쪽은 `rotate-180`)와 하단 점 인디케이터로, 모바일은 좌우 터치 스와이프(30px 이상 이동 + 가로 이동이 세로보다 클 때 스와이프로 판정)로 넘겨볼 수 있음. 전환 시 인스타그램 캐러셀처럼 모든 사진을 필름스트립으로 나란히 두고 컨테이너를 `translateX`(300ms ease-out)로 미는 방식이라 부드럽게 슬라이드됨(`<img src>`만 바꾸거나 `key`로 매번 재마운트하는 방식은 각각 "즉시 전환"·"깜빡임" 문제가 있어서 폐기됨). 화살표/스와이프 조작은 `stopPropagation`·`preventDefault`로 카드 클릭(상세 이동)과 분리되고, 일반 탭/클릭은 그대로 상세 페이지로 이동함. Google이 사진에 "음식 사진"/"가게 사진" 같은 카테고리 라벨을 안 줘서, 순서는 Google이 반환한 그대로이고 음식/외관을 앞뒤로 재배열하는 로직은 없음.
- `AuthContext`의 `user` 객체는 `email` 외에 `nickname`(선택), `avatar`(선택, 파일 업로드 결과를 FileReader로 변환한 base64 data URL)를 가질 수 있음. `updateNickname(nickname)` / `updateAvatar(dataUrl)`로 갱신. 아바타 업로드는 `MyPage.jsx`의 `NicknameModal`에서 jpg/png/webp, 500KB 이하로만 제한하고 실제 파일 스토리지 없이 `localStorage`에 그대로 문자열로 저장함 — 큰 이미지를 반복 업로드하면 localStorage 용량 한도에 걸릴 수 있음을 유의.
- `AuthContext`의 `scrapIds` 초기값은 빈 배열이 아니라 `DEMO_SCRAP_IDS`(`sushi-masa`, `ichiran-osaka`, `okonomiyaki-mizuno`) — `localStorage`에 `gurume_scrap_ids` 키가 이미 있으면 그 값이 우선하고, 키가 아예 없는 최초 방문에서만 데모 데이터가 채워짐.
- 인증은 `src/lib/supabaseClient.js`(`import.meta.env.VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY`, 값은 `.env`에 있고 gitignore 처리됨 — `.env.example`이 템플릿)를 통해 Supabase Auth와 실제로 연동됨. `AuthContext`의 `login(email, password)`/`signup(email, password)`/`logout()`/`changePassword(current, next)`가 전부 `supabase.auth.*` 실제 호출이고, 세션은 `onAuthStateChange`로 유지됨 — `ProtectedRoute`는 초기 세션 로딩 중(`loading === true`)에는 `/login`으로 잘못 리다이렉트하지 않도록 가드함. `updateNickname`은 `profiles.nickname`에 실제로 저장되지만, `updateAvatar`와 스크랩(`scrapIds`)은 여전히 localStorage 전용(아바타는 계정별로 `gurume_avatar_${user.id}` 키 사용). 회원가입 시 `auth.users` insert에 붙은 `handle_new_user` 트리거가 `profiles` 행을 자동 생성함.
- `HomePage.jsx`의 `RECENT` 배열은 현재 빈 배열(`[]`)로 되어 있어 하단 "이전 서치 결과" `<section>` 자체가 렌더링되지 않음(빈 배열이면 `RECENT.length > 0` 조건이 false). 그 안에 있던 인기 검색어(`POPULAR`) fallback 카드 UI는 지우지 않고 JS 블록 주석으로만 보존해뒀으니, 나중에 필요하면 주석만 해제할 것. 이 `RECENT`(하단 카드 섹션, 항상 빈 배열)와 아래 설명하는 **모바일 전용 검색 기록 오버레이**(`recentSearches`, 실제 데이터 있음)는 서로 다른 기능이니 혼동하지 말 것.
- **모바일 검색 기록 오버레이**(2026-08-18 추가): `HomePage.jsx`에서 `md` 미만 화면에서 검색 인풋(`SearchAutocompleteInput`)에 포커스하면 `mobileSearchOpen` state로 `fixed inset-0 z-50 bg-white` 전체화면 오버레이가 열림(PC는 기존 드롭다운 자동완성만 뜨고 이 오버레이는 `md:hidden`이라 안 뜸) — 뒤로가기 아이콘 + 자체 입력 폼 + `recentSearches` 목록(검색어 · 날짜 `MM.DD.` · X 삭제 버튼). `recentSearches`/`addRecentSearch`/`removeRecentSearch`/`clearRecentSearches`는 `AuthContext.jsx`에 있고, `scrapIds`와 동일한 이원화 패턴(로그인 시 `search_history` 테이블, 비로그인 시 `localStorage` 키 `gurume_recent_searches`)을 씀. `search_history`는 RLS로 `SELECT`/`INSERT`만 있었고 개별 삭제(X 버튼)를 위해 `DELETE` 정책(`search_history_delete_own`)을 추가함. **주의**: `AuthContext`의 세션 로딩 이펙트에서 `session?.user?.id`가 없다고 무조건 `setRecentSearches([])`를 호출하면 페이지 이동마다 `AuthProvider`가 재마운트되며 비로그인 사용자의 저장된 기록이 매번 사라지는 버그가 났었음(로그인 여부 판별과 "로그인→로그아웃 전환" 판별을 `hadSession` state로 분리해서 해결) — 이 패턴을 건드릴 때 같은 실수를 반복하지 말 것. `SearchAutocompleteInput.jsx`에 `onFocus` prop이 이때 추가됨(기존 `setOpen(true)` 동작 뒤에 추가로 호출).
  - **오버레이 내부 폼의 돋보기 버튼이 카카오톡 인앱 브라우저 등 일부 웹뷰에서 안 보이는 버그**(2026-08-18 수정): 원인은 두 가지 조합 — ① `src/components/icons/index.jsx`의 아이콘들이 실제 `width`/`height` 속성 없이 Tailwind className(`w-5 h-5`)에만 의존하고 있어서 CSS 로딩 타이밍에 따라 SVG가 크기 0으로 렌더링될 수 있었음 → `base()` 헬퍼에 기본 `width: 24, height: 24`를 추가(className이 뒤에 스프레드되므로 있으면 그쪽이 우선, 없을 때만 폴백). ② 오버레이 폼의 `flex-1` 인풋에 flexbox 기본값 `min-width: auto`가 걸려 있어서, placeholder 텍스트가 길면 인풋이 줄어들지 않고 옆의 검색 버튼을 화면 밖으로 밀어낼 수 있었음(뷰포트가 좁은 실기기에서만 발현, 데스크톱 브라우저 리사이즈 시뮬레이션으로는 재현 안 됨) → 인풋에 `min-w-0`, 양쪽 버튼에 `min-w-9 min-h-9`(고정 크기 강제)를 추가. 앞으로 아이콘이 "로컬/시뮬레이션에서는 보이는데 실기기에서만 안 보인다"는 리포트가 오면 이 두 가지(SVG 크기 속성 누락, flex 자식의 min-width 기본값)부터 의심할 것.
- `HomePage.jsx`의 hero 헤드라인 "광고 없는 찐맛집,"은 모바일 전용으로 `text-5xl`(원래 `text-4xl`에서 한 단계 키움, `sm:`/`md:`는 그대로 `text-5xl`/`text-6xl`)이고, 그 아래 "일본 미식여행을 위한 맛집 검색" 부제와 검색 인풋(`py-5`)은 원래 크기로 유지됨 — 부제·인풋을 모바일에서 줄였다가 다시 원복한 이력이 있으니 임의로 다시 줄이지 말 것. `#태그` 줄(`POPULAR.map`)은 모바일만 `leading-none`(PC는 `sm:leading-normal`)으로 줄 간격을 좁혀둠.
- `MyPage.jsx`의 이용약관/개인정보처리방침/비밀번호 변경/닉네임·프로필사진 수정/최근 검색어 모달은 전부 단일 `policyModal` state(문자열 키: `"terms"` `"privacy"` `"password"` `"nickname"` `"recentSearch"` `null`)로 어느 모달이 열려 있는지 관리함. 새 모달을 추가할 때도 별도 boolean state를 만들지 말고 이 패턴(키 문자열 하나 + 각 모달 컴포넌트가 자기 키인지 확인)을 따를 것.
- `Footer`(`src/components/Footer.jsx`)는 "Copyright(c)2026 GurumeTabi. All rights reserved." 문구를 보여주는 공용 컴포넌트로, 폰트 크기(14px)와 문구는 이 파일 하나에서만 관리하고 `className` prop으로 페이지별 색상/여백/정렬만 오버라이드함. 4개 화면(`HomePage`, `SearchResultsPage`, `ScrapPage`, `MyPage`)에 각각 다른 스타일로 배치되어 있음 — 색상은 홈이 `text-white/70`, 검색이 `text-[#999]`, 스크랩·마이페이지가 `text-white`로 서로 다르니 통일하려 하지 말 것. `SearchResultsPage.jsx`·`ScrapPage.jsx`는 `mt-auto`를 줘서, 콘텐츠가 스크롤 안 생길 만큼 짧아도(검색 전/스크랩 적음) Footer가 화면 하단에 붙게 해둠 — `MyPage.jsx`는 콘텐츠가 항상 충분히 길어서 `mt-auto` 없이도 자연스럽게 하단에 위치함.
- Tailwind v4에서 반응형(`md:`) 유틸리티와 비반응형 유틸리티를 같은 속성에 섞어 쓸 때(예: `p-8 pb-0`) JSX 소스 순서가 아니라 Tailwind가 생성한 CSS 소스 순서(반응형 variant가 항상 뒤에 옴)가 우선한다 — `md:p-8`이 있는데 하단 padding만 지우려면 `pb-0`이 아니라 `md:pb-0`처럼 같은 breakpoint 접두사로 오버라이드해야 실제로 적용됨. `ScrapPage.jsx`/`MyPage.jsx`의 Footer 하단 padding 처리에서 이 패턴을 씀(`p-6 pb-0 md:p-8 md:pb-0`).
- `SearchResultsPage.jsx`의 결과 영역(오른쪽, 필터 aside 제외) 스크롤 컨테이너는 `overflow-y-scroll`(auto 아님)로 항상 스크롤바 트랙을 예약해둠 — 그리드/리스트 뷰 전환 시 콘텐츠 높이가 달라져도 스크롤바 유무로 카드 폭이 흔들리지 않게 하기 위함. `.pretty-scroll` 클래스(`src/index.css`)도 트랙 배경을 옅은 흰색(`rgba(255,255,255,0.15)`)으로 채워 스크롤 여부와 무관하게 라인이 항상 보이도록 함. `MyPage.jsx`/`ScrapPage.jsx`도 페이지 전체를 `h-screen overflow-hidden`으로 고정하고 콘텐츠만 이 `overflow-y-scroll pretty-scroll` 패턴을 그대로 씀 — 세 화면 모두 동일한 스크롤 처리 방식.
- **`overflow-y-scroll` 컨테이너 안에 있는 요소에는 음수 `margin-top`을 주지 말 것.** `MyPage.jsx`/`ScrapPage.jsx`에서 콘텐츠를 위로 당기려고 스크롤 박스 안쪽 요소에 `-mt-[...]`를 줬다가, 스크롤 박스의 `scrollTop`은 0 밑으로 못 내려가서 그 요소가 스크롤 박스 top 위쪽(음수 좌표)으로 밀려나 영구히 안 보이고 스크롤로도 못 꺼내오는 버그가 났음(타이틀이 통째로 사라짐). 그래서 두 페이지 모두 계정 아이콘(`AccountActions`)을 스크롤 박스 **밖**, 콘텐츠 컬럼에 `absolute top-0 right-0 z-20`로 겹쳐 배치하는 방식으로 우회함 — 콘텐츠와 계정 아이콘 사이 간격을 좁히고 싶으면 스크롤 박스 안쪽 요소의 음수 margin이 아니라 이 absolute 오버레이의 위치·패딩을 조정할 것.
- `SearchResultsPage.jsx`의 **정렬(`sortBy`)과 그리드/리스트 뷰(`viewMode`)도 URL 쿼리(`?sort=`, `?view=`)에 반영됨**(2026-08-18 수정) — 원래 로컬 state로만 관리돼서, 상세 페이지 진입 후 뒤로가기로 이 페이지가 재마운트되면 "리뷰 많은 순"으로 정렬해놨어도 기본값("평점 높은 순")으로 리셋되는 버그가 있었음. 다른 필터들(`filterStateFromSearchParams`/`filterStateToSearchParamEntries`)과 같은 이유로 URL 기반 복원 패턴을 씀 — 기본값(`sortBy: "rating"`, `viewMode: "grid"`)일 때는 쿼리 파라미터 자체를 지워서 URL이 지저분해지지 않게 함. `setSortBy`/`setViewMode`는 이제 단순 `useState` setter가 아니라 URL도 함께 갱신하는 래퍼 함수이니, 정렬/뷰 상태를 바꿀 새 코드를 추가할 때 `setSortByState`(내부용) 대신 반드시 이 래퍼를 쓸 것.
- `SearchResultsPage.jsx`의 필터 사이드바는 **draft/applied 이중 상태**로 동작함 — 평점(`ratingMin`)·현지인 비율(`localMin`)·영업 상태(`openStatus`)는 전체/단계별 값 중 하나만 고르는 단일 선택 세그먼트(공용 `FilterSegmentGroup` 컴포넌트, 버튼은 `flex-none whitespace-nowrap`이라 텍스트 길이만큼만 차지하고 줄바꿈 없이 한 줄에 들어감 — 예전에 `flex-1`로 균등폭을 줬다가 짧은 라벨("전체")이 불필요하게 늘어나 보여서 되돌림), 추가 필터(카드 결제·예약 가능·역에서 도보 10분 이내, 이 순서)는 다중 선택 가능한 pill 버튼(`flex-wrap`, 텍스트 길이만큼만 차지). 체크박스·세그먼트를 고르는 동안은 `draft` state에만 반영되고, 실제 검색 결과 필터링(`matchedResults`)에는 `applied` state가 쓰이며, 하단 "필터 적용하기" 버튼(`applyFilters`)을 눌러야 draft→applied로 반영됨 — "초기화" 버튼(사이드바 상단, "필터" 타이틀 옆)은 draft·applied 둘 다 즉시 리셋. 예전에 있던 "불친절 후기 제외" 토글은 UI에서 빠졌고, `hasRudeReview`인 곳은 토글 없이 항상 결과에서 제외되는 숨은 기본 동작이 됨.
- **지역 필터는 완전히 삭제됨**(사이드바에 더 이상 지역 체크박스가 없음) — 검색어(자연어 검색창)와 지역 필터가 동시에 있으면 AND 조건이라 "오사카 검색 + 후쿠오카 필터"처럼 서로 다른 지역을 동시에 고르면 결과가 뒤엉키는 버그가 있었음. 이제 지역은 오직 검색창으로만 다루고(예: "오사카 라멘"), 사이드바는 음식종류·평점·현지인비율 등 순수 속성 필터만 남음. 지역 필터가 있던 세로 공간은 음식종류 필터(`FOOD_TYPES` 체크박스 목록)가 `flex-1`로 흡수해서 늘어나 있음.
- "역에서 도보 10분 이내" 필터는 `walkMinutes`가 null이면(아직 `fill-walk-minutes`를 안 돌린 지역) 항상 통과하도록 null-safe 처리되어 있음(`r.walkMinutes != null && r.walkMinutes > 10`). 오키나와는 애초에 이 필드가 항상 null(SKIP_REGIONS)이라 이 필터를 걸어도 오키나와 결과는 안 걸러짐 — 의도된 동작.
- **"더 보기" 페이지네이션**: 검색 결과는 처음에 8개(`RESULTS_PAGE_SIZE`)만 보여주고, "더 많은 "{검색어}" 맛집 보기" 버튼(`hasMore`가 true일 때만 노출)을 누르면 8개씩 누적 증가(`visibleCount`). 검색어/필터/정렬이 바뀌면 자동으로 8개로 리셋됨. `xl` 이상 화면에서는 그리드 6/7번째 카드(2번째 행 2·3열) 경계에 걸치도록 `absolute` 배치(`bg-brand-navy/80`, 흰 글자, `IconChevronDown` 아이콘)되고, 그 미만 화면에서는 `xl:hidden`인 일반 중앙 정렬 버튼으로 폴백됨 — 좁은 화면에선 카드 열 배치가 달라져 절대위치 좌표가 안 맞기 때문.
- **`fetchRestaurants()`는 반드시 페이지네이션을 거쳐야 함**(2026-08-18 수정) — Supabase(PostgREST)는 `.select('*')`만 쓰면 명시적 range 없이는 기본 1000행까지만 반환한다. `restaurants` 테이블이 1,970행으로 1000행을 넘어선 뒤로, `region`/`category` 정렬 순서상 뒷부분에 있던 지역·카테고리 조합이 검색 결과에서 통째로 빠지는 버그가 있었음(예: "오사카" 검색이 실제 193건 중 149건만 나옴 — 나머지 44건이 1000행 밖에 있었음). `src/lib/restaurants.js`의 `fetchRestaurants()`가 `.range(from, from+999)`로 1000행씩 순회해 전체를 모아오도록 고쳐져 있음 — 이 함수를 수정할 때 다시 단순 `.select('*')`로 되돌리지 말 것. `fetchRestaurantById`(단건 조회)와 `fetchBackupPlan`/`fetchNearbyDessert`(region+category로 좁혀서 조회, 결과가 애초에 적음)는 이 문제와 무관해서 페이지네이션 불필요.
- **검색 결과 개수 표시(`{N}곳 중 상위 {M}곳`) 조건은 `hiddenCount`가 아니라 `results.length < cappedResults.length`로 판정해야 함**(2026-08-18 수정) — `hiddenCount = matchedResults.length - cappedResults.length`는 매칭이 `MAX_RESULTS`(100)를 초과했을 때만 0보다 커지는 값이라, 매칭이 9~100건 사이(가장 흔한 케이스)일 때는 "더보기" 버튼이 있는데도 그냥 전체 매칭 수 없이 `results.length`(현재 8/16/24...)만 표시되어 사용자가 실제로 몇 건이 있는지 알 수 없는 버그가 있었음. `cappedResults.length`(최대 100건까지의 실제 매칭 수)와 `results.length`(현재 펼쳐진 개수)를 직접 비교하는 게 맞음.
- **백업 플랜**(`fetchBackupPlan`, `src/lib/restaurants.js`)은 더 이상 무작위 추천이 아님 — 원래는 지역·카테고리 조건 없이 `status='open'`인 아무 가게나 `.limit(1)`로 가져와서, 도쿄 라멘집 상세페이지에서 뜬금없이 인천공항 매장이 뜨는 버그가 있었음. 지금은 **같은 지역 + 같은 음식종류** 후보 중 위경도 기준 하버사인 거리로 가장 가까운 곳을 추천함. 함수 시그니처가 `excludeId`(문자열) 대신 `restaurant` 객체(`{id, region, category, lat, lng}`) 전체를 받으므로 헷갈리지 말 것.
- **근처 디저트 맛집**(`fetchNearbyDessert`, `src/lib/restaurants.js`, 2026-08-18 추가)은 `fetchBackupPlan`과 같은 하버사인 거리 로직이지만 음식종류 조건 없이 **같은 지역 + `category='디저트'`** 후보만 찾음. `RestaurantDetailPage.jsx`에서 "이 가게 저장하기" 버튼 옆에 나란히 배치되고(`restaurant.category === '디저트'`인 가게 자기 자신은 버튼 자체를 숨김), 후보가 없으면(`dessertChecked`가 true인데 `nearbyDessert`가 null) "근처 디저트 없음"으로 대체 표시됨 — 로딩 중(`dessertChecked` false)에는 백업 플랜처럼 즉시 "대안 없음"을 보여주지 않고 결과가 올 때까지 아무것도 렌더링하지 않음(깜빡임 방지). 색상은 로고의 벚꽃/별과 동일한 진한 핑크(`--color-brand-pink: #ee7191`, `index.css`)를 새로 추가해서 씀 — 기존 `brand-coral`(보라 계열 `#a855f7`)과는 다른 색이니 헷갈리지 말 것. 아이콘은 `IconCake`(`src/components/icons/index.jsx`에 신규 추가).
- 정렬 옵션(`SORT_OPTIONS`)은 **평점 높은 순 · 현지인 비율 높은 순 · 리뷰 많은 순**(`reviewCount` 내림차순) 3개뿐 — 예전에 있던 "추천순"(`trustScore` 가중합 정렬 함수)은 제거됐고, 기본 정렬값은 `rating`("평점 높은 순")임.
- 가게 사진이 없을 때(`restaurant.image`가 falsy) placeholder는 배경색 `#f9f8fc` + `public/noImage.png`(가게 아이콘, 608×469 비율 — **정사각형이 아니라서 `object-contain` 없이 정사각형 박스에 넣으면 가로로 눌려 보임**, 반드시 `object-contain`과 같이 쓸 것) 조합임. `SearchResultGridCard.jsx`(그리드)는 아이콘(128px) 아래 "사진 준비중"(`#666`)·"조금만 기다려주세요!"(회색) 텍스트가 같이 나오지만, `SearchResultCard.jsx`(리스트)는 썸네일 폭이 150px로 좁아서 아이콘만(112px) 보여주고 텍스트는 없음 — 그리드에만 텍스트가 있는 게 의도된 차이이니 리스트에도 맞춰 텍스트를 넣으려 하지 말 것.
- `RestaurantDetailPage.jsx`의 히어로도 사진이 없으면 `public/defaultHero.png`(후지산+성+벚꽃 사진)를 기본 배경으로 씀 — `objectPosition: 'center calc(50% - 200px)'`로 크롭 위치를 위로 올려 하늘/성이 보이게 맞춘 값이라, 이 이미지를 다른 사진으로 교체하면 오프셋도 그 구도에 맞게 재조정해야 함. 실제 사진이 있을 때만 검정 그라디언트 오버레이(`from-black/30 via-black/30 via-70% to-transparent`)가 나타나고, 실제 사진/기본 이미지 두 `<img>` 모두 하단 30%를 `mask-image: linear-gradient(to bottom, black 70%, transparent 100%)`로 페이드시켜서 이미지 하단이 흰 배경이 아니라 body의 실제 고정 그라디언트 배경으로 자연스럽게 이어지도록 함 — 별도의 단색(흰색 등) 그라디언트 div를 얹는 방식은 이미 시도했다가 배경색이 body 그라디언트와 안 맞아 부자연스러워서 폐기된 방법이니 다시 쓰지 말 것. 가게 이름은 `absolute inset-0 flex items-center justify-center`로 히어로 정중앙에 위치(과거엔 `translate-y-4`로 아래로 밀었었는데 요청으로 원복함 — 다시 오프셋을 주지 말 것). 뒤로가기 버튼은 "검색결과" 텍스트 없이 `IconArrowLeft` 아이콘만 있는 원형 버튼(`w-8 h-8 md:w-10 md:h-10`, `rounded-full`)이고, 그 아래 본문 카드(`bg-white rounded-t-3xl`)의 좌우·상하 padding은 모바일 `p-3`(PC의 절반), PC는 `md:p-6` 그대로 — 모바일 padding을 다시 키우려 하지 말 것.
- `SearchResultGridCard.jsx`/`SearchResultCard.jsx`의 스크랩(⭐) 버튼: 카드 전체에도 썸네일 확대(`group-hover:scale-110`)용 이름 없는 `group`이 걸려 있어서, 버튼에도 이름 없는 `group`을 쓰면 카드 아무 곳에 hover해도 별이 활성화되는 버그가 났었음 — Tailwind의 `group-hover:`는 "가장 가까운 group"이 아니라 "그 클래스를 가진 아무 조상이나 hover되면" 발동하는 선택자라서 두 `group`이 충돌함. 그래서 버튼에는 이름 있는 `group/star`를 쓰고 아이콘은 `group-hover/star:stroke-brand-coral`로 스코프를 좁혀서, 별 아이콘(버튼) 자체에 hover했을 때만 활성화되도록 고정함. 원형 배경(`bg-white/90 rounded-full`)은 제거됐고 아이콘만 24px(`w-6 h-6`, `strokeWidth={1}`로 얇게)로 커져 있음.
- `RestaurantDetailPage.jsx`의 위치 지도는 OpenStreetMap이 아니라 **Google Maps Embed API**(`googleMapEmbedSrc`, `VITE_GOOGLE_MAPS_EMBED_API_KEY` 환경변수)를 씀. "구글 지도" 버튼 링크는 `restaurant.googlePlaceId`가 있으면 `query_place_id` 파라미터를 붙여서 정확히 그 가게가 선택된 상태로 열리게 함(place_id 없이 좌표만 쓰면 Google이 정확한 가게가 아니라 좌표 위치만 보여줌). **주의**: place_id로는 Google Maps의 "리뷰 탭"까지 직행시킬 수 없음(그러려면 Places API가 안 주는 CID라는 별도 식별자가 필요) — 항상 "개요" 탭으로 열리는 게 정상 동작이고 버그가 아님.
- **리뷰 작성자 이름 마스킹**: `src/utils/maskReviewAuthor.js`가 순수 한글 2~4자 + 흔한 한국 성씨(약 60개 목록)로 시작하는 이름만 "성+이름 앞글자만 남기고 나머지 ○"로 마스킹함(예: "유수아"→"유수○"). 닉네임("가온아빠", "빼꼼")·영문/일본어 이름·5자 이상 이름은 그대로 노출됨 — `ReviewCard.jsx`에서만 적용되고 DB 원본은 안 바뀜.
- **리뷰 카드는 출처에 따라 다른 곳으로 이동하는 클릭 가능한 링크임**: `ReviewCard.jsx`에서 구글 리뷰는 `googlePlaceId` 기반으로 그 가게의 Google Maps 페이지, 네이버 리뷰는 `review.link`(네이버 블로그 검색 API의 `item.link`, 원문 글 URL)로 각각 카드 전체가 `<a target="_blank">`가 됨(`isNaver ? review.link : googleReviewsUrl`). 둘 다 없으면(예: `link` 백필 전 수집된 구버전 네이버 리뷰) `div`로 폴백되어 `cursor-pointer` 없이 클릭 불가 상태가 됨 — 버그가 아니라 의도된 폴백. "클릭하면 구글맵 가게 페이지로 이동해요 — 상단 '리뷰' 탭을 눌러 전체 리뷰를 확인하세요" 안내 문구는 리뷰 카드마다 반복 노출하면 중복이 심해서 `RestaurantDetailPage.jsx`의 `#지역 #카테고리 #불친절후기없음` 태그 라인 오른쪽에 1회만 표시하도록 옮겨져 있음(`ReviewCard.jsx`에는 이제 없음, 이 문구는 구글 리뷰 기준 안내라 네이버 리뷰 클릭 시 동작과는 별개).
- **리뷰 노출 순서는 랜덤으로 섞임**(2026-08-18 추가): `restaurants.js`/`reviews_cache` 조회 결과가 항상 구글 리뷰 먼저, 네이버 리뷰 나중 순서로 오길래(DB 삽입 순서를 그대로 따름 — `sync-restaurants`가 먼저 upsert하고 `sync-naver-reviews`가 나중에 insert함) `RestaurantDetailPage.jsx`에서 `shuffle()`(Fisher-Yates)로 섞은 `shuffledReviews`를 `useMemo(() => shuffle(restaurant?.reviews ?? []), [restaurant])`로 만들어 렌더링에 씀 — 의존성이 `restaurant`라서 페이지를 새로 불러올 때만 다시 섞이고, 리렌더링(리뷰 더보기 클릭 등) 중에는 순서가 유지되어 사용자가 읽던 리뷰가 갑자기 움직이지 않음. `restaurant.reviews`를 직접 렌더링하는 코드를 새로 추가하지 말고 이 `shuffledReviews`를 쓸 것.
- **리뷰 본문 line-clamp + "더보기" 인라인 버튼**: `ReviewCard.jsx`는 PC 3줄/모바일 5줄로 리뷰를 자르고, 넘칠 때만 텍스트 마지막 줄에 "...더보기"를 실제 문자열 잘라내기(`Intl.Segmenter`로 그래핌 단위 — 이모지 서로게이트 페어가 잘리는 걸 방지)로 인라인 삽입함. `line-clamp` CSS만으로는 잘리는 지점에 커스텀 마크업(버튼)을 끼워넣을 수 없어서(float 버튼은 `-webkit-line-clamp` 계산에서 무시되어 렌더링 자체가 안 되는 브라우저 버그가 있었음) 화면에 안 보이는 측정용 엘리먼트로 실측 후 자르는 방식을 씀. 폰트 크기(모바일 `text-sm`/PC `text-base`), line-height도 모바일 전용으로 한 단계씩 낮춰져 있음(PC는 원래 크기 유지).
- `SearchResultsPage.jsx`의 로딩 화면(`loading === true`일 때)은 흰 배경 카드가 아니라 **페이지 배경 그라디언트가 그대로 비치는 상태**에서 `public/simbol.png`(브랜드 심볼, `w-48 h-48 opacity-10`)과 `text-brand-navy/40` 텍스트를 세로로 배치함 — `bg-white rounded-2xl` 같은 배경 박스를 다시 씌우려 하지 말 것(의도적으로 제거됨).
