# PRD: Gurume Tabi (일본 여행 맛집 리뷰 플랫폼)

> 작성자: 이고은 · 바이브코딩 기반 디지털플랫폼 제작 1~7단계 워크시트 종합
> 이 문서는 학습/실습 목적의 MVP 기획안입니다. TAM/SAM/매출 수치는 AI 추정치이며 실제 수치·법률·기술 검증은 별도로 필요합니다.

---

## 1. 개요

**서비스명**: Gurume Tabi (구루메 타비, 가칭: 일본 찐맛집 검증 & 백업 내비게이터)

**컨셉 스테이트먼트**
> 광고성 리뷰와 우발적 휴무로 맛집 선택에 실패하고 싶지 않은 2030 일본 자유여행객을 위해, 흩어진 리뷰를 자동 필터링해 한국인 입맛에 맞춰 검증된 찐맛집을 한곳에서 보여주고, 현지 상황 변화 시 즉시 근처 백업 플랜(2순위 맛집)을 추천하여 실패 없는 여행 경험과 탐색 시간 절약을 제공하는 서비스

**핵심 가치**: 신뢰(광고에 속지 않는다) + 안심(대안이 항상 있다)

---

## 2. 배경 및 문제 정의

### 2.1 문제
광고성 정보와 관광객 전용 식당이 너무 많아, 진짜 현지 맛집을 골라내기 위한 정보 탐색 시간이 과도하게 소요됨. 최근 엔저로 일본 방문 한국인이 급증하면서 이 문제를 겪는 사람이 늘고 있음.

### 2.2 근본원인 (5 Whys)
광고성 정보의 난립과 언어·감성 장벽으로 인해, 한국인 2030 여행객이 신뢰할 수 있는 '검증된 현지 찐맛집'만 빠르게 필터링해 주는 전용 서비스가 부재하다.

- 블로그/인스타 리뷰가 너무 많고 광고·협찬글이 섞여 있음
- 포털/SNS 알고리즘상 협찬 콘텐츠가 상위 노출되기 쉬움
- 상위 노출 정보만으로는 관광객 전용 식당인지 '찐 맛집'인지 판별 불가
- 현지인 리뷰 사이트(타베로그 등)는 일본어 장벽, 구글맵은 한국인 입맛과 평점 기준이 다름
- 결과적으로 광고 배제 + 한국인 2030 감성에 맞춘 전용 맛집 정보 서비스가 없음

### 2.3 HMW (How Might We)
1. 어떻게 하면 2030 여행객이 광고성 정보 없이 신뢰할 수 있는 '진짜 현지 맛집'만 1초 만에 확인하게 할 수 있을까?
2. 어떻게 하면 한국인 대상 광고 마케팅을 어려워하는 일본 현지 식당 사장님이 2030 한국인 손님을 자연스럽게 유치하게 할 수 있을까?
3. 어떻게 하면 사전 계획 없이 현지에 도착한 여행객이 지금 내 위치 주변의 실패 없는 맛집을 즉시 안내받고 예약까지 끝내게 할 수 있을까?

---

## 3. 타겟 사용자

### 3.1 최종 페르소나
| 항목 | 내용 |
|---|---|
| 이름 / 나이 / 직업 | 김민준 / 34세 / IT 개발자 |
| 라이프스타일 | 광고보다 데이터와 실제 방문 후기의 객관적 검증을 중시하는 실용주의자 |
| 목표 | 바이럴 광고를 배제한 진짜 맛집 발굴, 구글 평점 필터링·리뷰 정독으로 실패 확률 최소화 |
| 페인포인트 | 광고성 칭찬 리뷰만 믿고 갔다가 실망, 타베로그 등 일본 현지 앱은 평점 수가 적거나 한국인 입맛과 안 맞음, 불친절하거나 웨이팅 기준이 불명확한 매장 |
| 행동 습관 | 구글 평점 3.5 이상을 1차 기준, 불친절 후기 있으면 제외, 리뷰 정독 후 방문 결정, 항상 백업 플랜(2순위 선택지) 준비 |
| 기술 친숙도 | 중상 (평점 필터링, 정독 후 선별, 목적별 앱 조합에 능숙) |

### 3.2 핵심 페인포인트 (인터뷰 기반)
- 일본 특유의 결제 수단 장벽: 현금 전용(Cash Only) 매장, PayPay 등 특정 간편결제만 지원해 당황
- 현지 예약 시스템 접근성/노쇼 부담: 일본 전화번호 요구, 번역 오류, 취소 수수료 불안감으로 예약을 꺼림

---

## 4. 시장 및 경쟁 분석

### 4.1 시장 규모 (1단계 AI 추정치 — 검증 필요)
- TAM: 연간 약 380만 명 (한국인 2030 방일 여행객)
- SAM/SOM: 1년 차 약 10만 명(점유율 2.5~3%), 2년 차 약 30만 명(8~10%)
- 매출·순이익 추정(1년 차): 매출 3.1억 / 총비용 2.3억 / 순이익 약 8천만 원(순이익률 ~25%) — **AI 모의 추정치, 실제 수치 아님**
- 재검증 방법(7단계): 한국관광공사(KTO)·일본정부관광국(JNTO) 통계로 TAM 재확인, 유사 앱 다운로드/MAU 지표로 SAM/SOM 비교 필요

### 4.2 경쟁 벤치마킹
| 서비스 | 핵심 기능 | UX 약점 | 수익모델 |
|---|---|---|---|
| 타베로그 (직접경쟁) | 현지인 중심 식당 검색·예약 | 한국인 입맛과 평점 기준 차이, 번역 UX 부자연스러움 | 점주 광고/수수료, 유료 멤버십 |
| 트리플 (직접경쟁) | 맛집 목록·일정 연동 | 상단 노출 광고성 후기 우려, 실시간 대안 제시 부족 | 제휴 숙박/투어/티켓 수수료 |
| 구글 지도 (간접경쟁) | 장소 검색, 평점/리뷰 | 광고성 바이럴 리뷰 선별 어려움, 맛집 전용 딥데이터 부족 | 플랫폼 광고 |
| 네이버 블로그 (간접경쟁) | 여행기·맛집 후기 | 협찬글 다수, 정형 데이터 비교 어려움 | 검색/디스플레이 광고, 애드포스트 |

**차별점**: 광고성 리뷰를 자체 필터링하고, 한국인 입맛에 검증된 현지 맛집과 영업 중단 시 즉시 이동할 수 있는 근처 백업 플랜(2순위 맛집)을 즉각 추천하는 맞춤형 탐색 서비스.

### 4.3 리스크
- 계절성: 6~7월/12월 성수기 vs 3~4월·9~10월 비수기 갭
- 이탈률: 2030 세대는 여행 종료 후 앱을 삭제하는 성향이 강함
- 경쟁 강도: 트리플·마이리얼트립·클룩·야놀자 등 대형 OTA 존재
- 외부 변수: 자연재해, 엔화 환율 변동, 한일 관계 등 단일 국가 의존 리스크

---

## 5. 범위 (MoSCoW)

| 분류 | 기능 | 비고 |
|---|---|---|
| **Must** | 광고성 리뷰 자동 필터링 | 서비스 정체성의 핵심 |
| **Must** | 한국인 맞춤 맛집 검증 정보 통합 제공 (평점·현지인 비율·불친절 여부) | 여러 앱을 번갈아 확인하던 문제 해결 |
| **Must** | 실시간 근처 백업 플랜(2순위 맛집) 추천 | 경쟁사에 없는 차별화 기능 |
| Should | 결제 수단 사전 안내 (현금 전용/PayPay 등) | MVP 이후 |
| Should | 예약 시 번역·노쇼 수수료 안내 | MVP 이후 |
| Could | 방문 맛집 저장 및 '나만의 일본 맛집 지도' | 리텐션용, MVP 범위에 포함(스크랩 화면으로 구현) |
| Could | 내 근처 맛집(GPS 기반 주변 맛집 탐색) | 2026-08-24 구현 완료 — `/nearby`, 지도+리스트 양방향 연동. 백업 플랜(휴무 시 대안)과 달리 "지금 내 위치 주변 탐색" 자체가 목적 |
| Could | 현지 식당 사장님 대상 제휴/쿠폰 등록 기능 | MVP 검증 이후 |
| **Won't** | 여행 일정 전체 관리(숙소·교통 포함 종합 플래너) | 기존 OTA 영역, 범위 확장 방지 |

---

## 6. 정보구조 (사이트맵)

메인 탭 6개: **홈(검색 시작) · 검색 결과 · 내 근처 맛집 · 맛집 상세 · 스크랩 · 마이페이지**

| Depth 1 | Depth 2 | 포함 기능 |
|---|---|---|
| 홈 | 키워드/필터 검색바, 서브 페이지 헤더 검색폼 | 지역·음식 종류 검색 진입 (지도 없음) |
| 검색 결과 | 필터 바, 검증 맛집 리스트, 정렬/보기 전환 | 평점·현지인 비율·영업 상태는 단계별 값 중 하나만 고르는 단일 선택 세그먼트(전체/3.0+/3.5+/4.0+ 등) + 실용정보 필터(카드 결제·예약 가능·역에서 도보 10분 이내, 다중선택) + 음식 종류 필터(스시·라멘 등 21종, 체크박스·다중선택·내부 스크롤). **지역 필터는 없음** — 검색어와 지역 필터가 동시에 있으면 서로 다른 지역을 고를 때 결과가 뒤엉키는 문제가 있어 제거했고, 지역은 검색창(자연어)으로만 다룸. 검색창은 음식 종류+지역 통합 자동완성(예: "오"만 입력해도 "오사카"·"오코노미야키"가 함께 추천) 및 키보드 방향키/Enter 선택 지원, 텍스트 입력 시 지우기(X) 버튼 노출, 일본어 음차 표기 흔들림(오코노미야키/오꼬노미야끼 등) 자동 정규화 매칭. "맛집" 같은 수식어나 데이터에 없는 세부 표현(라멘 육수 종류 등)은 검색 시 무시되어 상위 카테고리로 폴백되고, "술집"/"돈까스" 같은 통용 표현은 실제 데이터 용어("이자카야"/"돈카츠")로 자동 치환됨(2026-08-19). 필터는 선택 즉시 반영되지 않고 하단 "필터 적용하기"를 눌러야 결과에 반영되며(상단 "초기화"는 즉시 반영), 불친절 후기 제외는 토글 없이 항상 적용되는 기본 동작. 정렬(평점 높은 순·현지인 비율 높은 순·리뷰 많은 순)·그리드/리스트 보기 전환은 검색 여부와 무관하게 항상 노출되며, 검색을 실행한 뒤에는 정렬 드롭다운 옆에 검색창이 추가로 나타남. 결과는 처음 8개만 보이고 "더 보기" 버튼으로 추가 로드(2026-08-19부터 개수 상한 없이 전체 매칭 결과를 이 방식으로 로드). 결과 카드(그리드·리스트 공통)에 `#지역 #음식종류` 해시태그 노출, 그리드 카드는 사진 여러 장을 화살표로 넘겨보는 캐러셀 지원, 사진 없는 가게는 기본 아이콘 placeholder로 대체 |
| 내 근처 맛집(2026-08-24 추가) | 실시간 위치 지도, 도보 10분 반경 맛집 리스트 | 진입 시 Geolocation 권한을 요청하고, 좌표가 일본 영역 밖이면 지도·리스트 대신 안내 문구만 노출(일본 여행 중에만 의미가 있는 기능이라 국내에서는 쓸 수 없음 — 이 판단은 위치 권한 승인 "후"에만 가능해서 메뉴 자체는 항상 노출됨). 왼쪽 지도는 내 위치(노란 원)와 도보 10분 반경(약 670m, 직선거리 기준) 이내 가게를 식당(보라 포크&나이프 핀)/카페·디저트·베이커리(로고 벚꽃색 커피컵 핀)로 구분해 마커로 표시, 마커 클릭 시 가게명·카테고리·도보시간·평점이 담긴 말풍선이 뜨고 가게명 클릭 시 상세 페이지로 이동. 오른쪽은 검색 결과와 동일한 카드 UI를 재사용(정렬·그리드/리스트 토글·카테고리 필터 포함), 카드 클릭 시 상세 이동 대신 지도 마커 선택 + 자동 스크롤로 양방향 연동됨 |
| 맛집 상세 | 히어로 배너, 사진 캐러셀, 리뷰 요약, 위치 지도 + 백업 플랜, 저장 버튼, 공유 | 히어로 이미지(사진 없으면 기본 배경 이미지로 대체, PC는 최대 260px로 높이 제한)에 어두운 오버레이(사진이 있을 때만) 위로 가게 이름을 중앙 표시, 좌우 상단에 뒤로가기/공유 버튼(모바일은 OS 공유 시트, PC는 링크 복사, 2026-08-19 추가). 콘텐츠는 왼쪽(사진 캐러셀 최대 5장 + 평점·현지인 비율·영업 상태 배지 + 지역·음식 종류·현지인 방문 다수·불친절 후기 없음 해시태그 + 주소·전화번호 + 저장 버튼)과 오른쪽(위치 지도 + 백업 플랜)으로 나뉘고 리뷰는 왼쪽 블록 아래 이어짐(2026-08-19 레이아웃 재구성). 네이버 블로그+구글 리뷰(출처별 hover 강조색 구분), 구글 지도 바로가기 버튼, 휴무/소진 시에만 백업 플랜 지도 노출. 검색·스크랩·내 근처 맛집 중 어디서 진입했는지에 따라 헤더 nav 하이라이트가 그에 맞게 표시됨(2026-08-24) |
| 스크랩 | 저장 리스트, 총 개수 표시 | 로그인 필요(ProtectedRoute로 접근 제한, scraps 기반). 타이틀 옆 "총 N개" 카운트. 목록 그리드는 화면 폭을 꽉 채우며 반응형(모바일 1열~xl 4열), 검색 결과와 동일한 카드 컴포넌트 재사용. 검색 결과 화면과 동일하게 페이지 자체는 고정 높이(스크롤 없음)이고 콘텐츠 영역만 내부 스크롤되며 스크롤바가 항상 표시됨(그리드/리스트 전환·콘텐츠 길이 변화에도 레이아웃이 흔들리지 않도록) |
| 로그인 | 이메일/비밀번호 + 구글 소셜 로그인 | Supabase Auth 이메일/비밀번호 + Google OAuth(2026-08-19 추가). 로그인 성공 시 원래 접근하려던 화면으로 리다이렉트 |
| 회원가입 | 이메일/비밀번호 폼 | Supabase Auth로 실제 가입 처리(이 프로젝트는 이메일 확인 절차가 꺼져 있어 가입 즉시 로그인됨) |
| 마이페이지 | 설정 | 모바일: 카드 스택형(프로필·활동·알림·계정·정보). PC(md 이상): 대시보드형 — 계정 정보 카드 + 알림 설정/계정 설정/검색 통계 3열 그리드 + 이용약관 및 정책. 프로필 수정(닉네임 + 프로필 사진), 비밀번호 변경, 로그아웃, 회원 탈퇴, 최근 검색어 조회(클릭 시 해당 검색어로 재검색, `search_history` 실데이터 연동 완료). 검색 통계(스크랩 개수·최근 검색 횟수)도 실데이터 기준으로 표시(2026-08-19). 알림 설정은 미구현 기능이라 "준비중" 배지와 함께 비활성화 상태로 노출(2026-08-19) |

> (2026-08-24 변경) 원래는 실시간 위치 지도를 맛집 상세의 백업 플랜에만 배치해 불필요한 위치 권한 요청을 최소화했으나, "내 근처 맛집" 기능 추가로 `/nearby` 진입 시에도 Geolocation 권한을 요청함 — 다만 사용자가 그 메뉴를 직접 눌렀을 때만 요청되고(자동/선제 요청 없음), 상세 페이지의 백업 플랜은 여전히 위경도 계산만 쓰고 브라우저 위치 권한을 요구하지 않음(기존 동작 유지).

### 6.1 핵심 유저 플로우 (시나리오: 가려던 맛집이 휴무일 때)
```
홈(검색) → 검색 결과(필터 적용) → 맛집 상세(영업 상태 확인)
   ├─ 영업중 → 예약/리뷰 확인 흐름
   └─ 휴무/소진 → 지도 + "근처 백업 플랜 보기" CTA → 백업 맛집 상세 → 방문 결정
                                                              └─ (선택) 스크랩에 저장
```
위치 권한은 백업 플랜 CTA를 누르는 시점에만 요청한다.

---

## 7. UI/UX 설계

**와이어프레임 (모바일+데스크톱, 5개 화면 + DB 설계 + 시스템 아키텍처 포함)**
https://claude.ai/code/artifact/b2aa2e21-ea95-4376-b332-f06f53530b39

### 7.1 브랜드 톤 & 컬러
| 항목 | 값 | 이유 |
|---|---|---|
| 브랜드 톤 | 신뢰/안심형 (Verified & Reliable) | '검증'이 핵심 가치 — 담백하고 믿을 수 있는 인상 우선 |
| 주색 (brand-navy) | 딥 퍼플 `#6D28D9` (다크 `#4C1D95`) | 객관적 데이터 기반 이미지, 실제 구현 시 네이비 대신 퍼플 계열로 확정 |
| 강조색 (brand-coral) | 퍼플 코랄 `#A855F7` (다크 `#7E22CE`) | 영업 상태 경고, 백업 플랜 CTA, 로그인 화면 오버레이 등 행동 유도 요소 전용 |
| 보조색 (brand-peach) | 라이트 라벤더 `#EDE4FB` (다크 `#DCC9F5`) | 카드 배경 강조, 구분선, hover 배경 |
| 상태색 | 영업중 `#2F9E5B` · 휴무 `#6B7280` · 재료소진 `#DC2626` | 영업 상태 배지 |
| 로고 | `public/logo.png` (후지산·벚꽃·덮밥 그릭 심볼 + "Gurume Tabi" 워드마크) | 헤더, 사이드바(width 180px, 텍스트 워드마크 대체), 로그인 화면에 적용해 전 화면 통일 |
| 히어로 배경 | 홈 `public/sushi.mp4`(영상, 브랜드 오버레이 `bg-brand-navy-dark/35`) · 로그인/회원가입 `public/loginBg.png`(이미지, 블러 없이 선명하게, 동일 오버레이) | 톤 통일을 위해 두 화면 모두 같은 오버레이 색/투명도 사용 |
| 인터랙션 | 홈 검색창 포커스 시 브랜드 보라(navy)·로고 벚꽃 분홍 2색 `conic-gradient` 4px 보더가 회전하는 애니메이션 | 검색 시작 지점에 시각적 강조를 주기 위한 마이크로 인터랙션 |

> Tailwind v4 `@theme`(`src/index.css`)에 CSS 변수로 정의. 폰트 크기 스케일은 기본값 대비 한 단계씩 상향 조정(`text-xs`=14px ~ `text-2xl`=28px)해 가독성을 높였다.

> **`design-system/MASTER.md`**: 위 토큰·컴포넌트 규칙을 `ui-ux-pro-max` 스킬로 정식 문서화(색상/타이포/여백/모션/접근성 체크리스트 + 화면별 적용 가이드). `design-system/mockups/`에 7개 화면 standalone HTML 목업도 함께 생성됨. 업종 일반 추천(마켓플레이스식 비비드 톤)이 아니라 페르소나 워크시트(3.1)의 신뢰/실용주의 톤을 우선해 채택.

### 7.2 공통 컴포넌트
- **맛집 카드**: 사진 썸네일 + 상호명 + 평점 배지 + 현지인 비율 아이콘
- **영업 상태 배지**: 영업중(그린)/휴무(그레이)/재료소진(레드). 맛집 상세에서는 평점 배지 옆에 나란히 배치
- **검증 리뷰 요약 태그**: `#현지인 방문 다수`, `#불친절 후기 없음` 등 — pill 배지가 아닌 해시태그 텍스트 형식
- **백업 플랜 CTA 버튼**: 코랄 강조색, 하단 고정(Sticky), 휴무/소진 시에만 노출
- **빈 상태(Empty State)**: 검색 결과 0건, 스크랩 0개일 때 아이콘 배지 + 안내문 + 재검색/필터초기화 CTA (배경색 없이 페이지 배경 위에 배치)
- **계정 액션(AccountActions)**: 프로필 아바타(44px 원형) + 로그아웃 아이콘 버튼 묶음. 헤더, 마이페이지·스크랩 화면에서 공통으로 재사용(두 화면은 콘텐츠 위에 겹쳐서 배치). 모바일 헤더가 메뉴 닫힘 상태일 때는 아바타만 노출하고 로그아웃은 숨김(모바일 메뉴 안으로 이동)
- **모바일 헤더 드롭다운 메뉴**: 햄버거(28px) 탭 시 아바타가 X로 바뀌고, 검색/스크랩/마이페이지 각 항목에 아이콘이 붙어 노출됨. 현재 화면은 연보라 배경으로 전체 행 하이라이트. 로그아웃(비로그인 시 로그인)은 메뉴 맨 아래 항상 연보라 배경으로 고정된 행으로 노출
- **푸터(Footer)**: "Copyright(c)2026 GurumeTabi. All rights reserved." 저작권 문구, 4개 화면(홈·검색 결과·스크랩·마이페이지) 최하단에 공통 배치. 화면 배경 톤에 맞춰 색상만 다르게 적용(어두운 배경엔 흰색, 밝은 배경엔 회색 계열)

---

## 8. 데이터베이스 설계 (Supabase)

> **프로젝트 생성됨**: `aeipftjmlikejemppxii` (https://aeipftjmlikejemppxii.supabase.co). Claude Code에 MCP로 연결되어 대화 중 직접 조회·작업 가능(full access). 아래 5개 테이블은 이 문서 기준으로 **실제 생성 완료**(RLS 적용)됨.

| 테이블 | 주요 컬럼 | 제약·관계 | 비고 |
|---|---|---|---|
| `profiles` | id(uuid, PK), email, nickname, avatar_url, is_admin(boolean, 기본 false) | id는 `auth.users(id)` 참조 FK, ON DELETE CASCADE. `auth.users` insert 시 `handle_new_user` 트리거로 자동 생성 | 이메일 로그인(Supabase Auth) 실제 연동 완료 — nickname은 마이페이지에서 실제로 갱신됨. avatar_url은 Supabase Storage 업로드 후 공개 URL 저장 예정 — 현재는 업로드한 이미지를 base64로 변환해 localStorage에만 저장(브라우저 새로고침 시에도 유지되나 기기 간 동기화 불가, 실제 파일 업로드는 백엔드 작업 시 구현). is_admin은 관리자 계정 구분용으로 추가했으나 이를 검사하는 관리자 화면/로직은 아직 없음 |
| `restaurants` | id(uuid, PK), name, address, phone, lat, lng, google_place_id, category, region, image_url, image_urls(배열), rating, review_count, tagline, status, local_ratio, has_rude_review, accepts_card, walk_minutes, nearest_station, accepts_reservation, opening_hours(jsonb), business_hours, regular_holiday, budget_dinner, budget_lunch, hotpepper_url, updated_at | google_place_id 유니크(중복 등록 방지) | **실제로 Google Places 데이터가 채워져 있음**(14개 지역 × 14개 카테고리, 약 4,700여 곳 — 2026-08-19에 이자카야·카레·오뎅·돈부리·텐동·소바·쿠시카츠·모츠나베·스키야키·장어덮밥·오무라이스·야키소바·몬자야키 13개 카테고리 약 2,139곳을 신규 수집해 6개→14개로 확장). 최초 설계보다 컬럼이 대폭 늘어남 — `local_ratio`는 여전히 null, `has_rude_review`/`accepts_card`/`walk_minutes`/`accepts_reservation`은 일부만 채워짐(아래 10.1 참고). `opening_hours`는 2026-08-19에 추가된 컬럼으로 Google `regularOpeningHours.periods`를 그대로 저장해 실시간 영업중 계산에 사용(10.3 참고), 전체의 약 66%만 채워짐(Google 자체에 시간 정보가 없는 가게는 제외) |
| `reviews_cache` | id(uuid, PK), restaurant_id(FK), source('naver'\|'google'), author, snippet, rating, fetched_at, is_ad_filtered, taste_snippet, link | restaurant_id → restaurants.id FK, ON DELETE CASCADE | **Google 리뷰 + 네이버 블로그 리뷰 둘 다 실제로 채워져 있음**(Google: 가게당 최대 5개, `is_ad_filtered` 항상 `false`. 네이버: `sync-naver-reviews`로 2026-08-18 전체 수집 완료, 1,470곳 총 6,598건, 검색 스니펫의 협찬 키워드 감지로 `is_ad_filtered` 채워짐) |
| `scraps` | id(uuid, PK), user_id(FK), restaurant_id(FK) | user_id+restaurant_id 조합 유니크 | "스크랩" 기능. ✅ **2026-08-19 실제 DB 연동 완료** — 로그인 시 이 테이블에 저장/조회(계정별 동기화), 비로그인 시에만 localStorage(`scrapIds`) 폴백 |
| `search_history` | id(uuid, PK), user_id(FK), keyword, searched_at | user_id → profiles.id FK, ON DELETE CASCADE | 마이페이지 "최근 검색" 카운트·목록 및 클릭 시 재검색 기능용. ✅ 실제 검색 시점 기록·저장·재검색 연동 완료, 마이페이지 통계 카운트도 이 실데이터를 사용(2026-08-19) |

**RLS 적용 완료**: `profiles`·`scraps`·`search_history`는 로그인한 본인 행만 SELECT/INSERT(profiles는 UPDATE도) 허용하는 정책이 실제 적용됨. `restaurants`·`reviews_cache`는 조회가 전체 공개(`select using (true)`)로 되어 있어 프론트가 익명 사용자로도 직접 SELECT 가능함. 쓰기(INSERT/UPDATE)는 서비스 role(Edge Function)만 하고 있고 클라이언트에는 허용 안 됨.

---

## 9. 기술 스택 및 아키텍처

| 구분 | 기술 | 역할 |
|---|---|---|
| 프론트엔드 | React, Vite | UI 구현, 개발 서버/빌드 |
| 스타일링 | Tailwind CSS v4 | 디자인 시스템(주색 `#6D28D9` brand-navy · 강조색 `#A855F7` brand-coral) 적용, `tailwind.config.js` 없이 `src/index.css`의 `@theme`로 설정 |
| 프론트 언어 | JavaScript | React 컴포넌트 로직 |
| 데이터 수집 백엔드 | **Supabase Edge Functions** (Deno/TypeScript) | 당초 계획했던 Flask 대신 채택 — 네이버 블로그·구글 Places API 호출, 리뷰 필터링 로직을 여기서 처리(아래 아키텍처 절 참고) |
| 데이터/인증 | Supabase | DB(위 5개 테이블, 실제 생성 완료, `restaurants`/`reviews_cache`는 실데이터로 채워짐) + Auth(이메일/비밀번호 로그인, 프론트에서 `@supabase/supabase-js`로 실제 연동 완료), RLS로 접근 제어 |
| 지도 | Google Maps Embed API + Maps JavaScript API(`@react-google-maps/api`) | 맛집 상세 페이지는 좌표 하나만 보여주면 되는 Embed API(iframe), "내 근처 맛집"(2026-08-24)은 여러 마커+클릭 정보창이 필요해 별도로 Maps JavaScript API를 로드(같은 API 키에 두 API를 함께 활성화) |

**실제 아키텍처 흐름 (Flask 대신 Supabase Edge Function 채택)**
```
React (Vite) ──SELECT (anon key, RLS)──> Supabase DB (restaurants, reviews_cache 등)
                                              ▲
                                              │ upsert (service role)
                                              │
                          Supabase Edge Functions (Deno/TS)
                              ├─ sync-restaurants: 구글 Places Text Search 호출 → restaurants/reviews_cache에 저장
                              ├─ sync-naver-reviews: NAVER API HUB 블로그 검색 → reviews_cache(source='naver')에 저장
                              ├─ flag-rude-reviews: reviews_cache를 스캔해 has_rude_review 갱신
                              ├─ fill-walk-minutes: Places Nearby Search로 도보거리 계산
                              └─ translate-restaurant-names: Cloud Translation API로 상호명 보정
```
당초 문서(7단계 워크시트까지)는 Flask 백엔드가 API 키를 보호하며 요청을 대신 처리하는 구조를 계획했으나, 실제 구현 단계에서는 **Supabase Edge Function이 그 역할을 그대로 대신함**(서버리스라 별도 배포·호스팅 관리가 필요 없고, 이미 Supabase 프로젝트에 통합돼 있어 채택). Google Places/Translation API 키는 Edge Function의 secret으로만 저장되고 프론트에는 절대 노출되지 않는다. 프론트는 Edge Function을 직접 호출하지 않고, Edge Function이 미리 채워둔 `restaurants`/`reviews_cache`를 anon key로 SELECT만 한다 — 즉 **사용자가 검색할 때마다 Google API가 호출되는 구조가 아니라, 관리자가 주기적으로 수집을 실행해 DB를 갱신하는 배치 방식**이다. 네이버 블로그 API(`sync-naver-reviews`)도 2026-08-18부터 같은 배치 방식으로 이 파이프라인에 연동되었다(10.1 참고).

**재수집 주기는 분기(3개월) 1회로 운영**(2026-08-19, 기존 월 1회에서 변경) — 전체 84개 지역×카테고리 조합(+디저트 14개) 재수집 1회당 Google Places Text Search + Cloud Translation API 비용이 약 9~10만 원 실측됨. 무료 크레딧 소진 속도를 늦추기 위해 주기를 늘렸으며, 매 재수집 전에는 항상 예상 비용과 잔여 크레딧을 확인 후 사용자 승인을 받는다(자동화 없음).

> 배포 환경(프론트 호스팅)은 아직 미정 — 추후 논의에서 확정.

---

## 10. 외부 연동

### 10.1 리뷰 데이터 (Must-have: 광고성 리뷰 자동 필터링)
| 소스 | 방식 | 상태 | 주요 제약 |
|---|---|---|---|
| 구글 Places API (Text Search + Place Details) | `sync-restaurants` Edge Function이 지역×음식종류 조합으로 검색해 가게 정보·평점·사진·리뷰(reviews 필드)를 `restaurants`/`reviews_cache`에 upsert | ✅ **실제 연동 완료** — 14개 지역 × 14개 카테고리, 가게당 리뷰 최대 5개까지 수집됨(약 4,700여 곳, 2026-08-19에 6개→14개 카테고리로 확장) | 가게당 리뷰 최대 5개, 영구 캐싱 금지 원칙에 따라 재수집 시 기존 google 리뷰를 지우고 새로 씀. 화면에 "Powered by Google" 출처 표기함(상세 페이지 리뷰 섹션 하단). Text Search가 순수 한글 지명을 잘 인식 못 해 지역명을 영어로 변환해서 보냄. 쇼핑몰/건물, 일본 밖 결과는 수집 단계에서 자동 제외(2026-08-15부터 국가 필터 추가 — 그 전 수집분엔 소재지가 다른 나라인 오염 데이터가 섞였던 이력 있음, 발견 시 삭제 완료) |
| 네이버 블로그 검색 API (NAVER API HUB) | `sync-naver-reviews` Edge Function이 가게명+지역으로 블로그를 검색해 스니펫의 협찬 키워드(협찬/제공받아/체험단 등)를 감지, `reviews_cache`에 `source='naver'`로 upsert. 검색 API 응답의 `item.link`(블로그 글 원문 URL)를 `link` 컬럼에 그대로 저장하고, 스니펫에서 맛 표현 키워드(맛있/쫄깃/바삭/가성비 등 25개)가 포함된 구절만 뽑아 `taste_snippet`에 저장 | ✅ **실제 연동 완료**(2026-08-18) — 전체 1,748개 가게 순회 호출, 1,470곳에 리뷰 붙음(총 6,598건, 협찬 감지 11건). `link`·`taste_snippet` 컬럼도 같은 날 추가해 기존 리뷰 전체 백필 완료(`link` 100%, `taste_snippet`은 맛 키워드가 매칭된 803건에만 채워짐 — 나머지는 원래 null) | 검색 스니펫만 제공 — 키워드 기반 필터라 본문 전체 LLM 정밀 분류(원래 계획)는 아직 아님. 본문 전체는 blog.naver.com 개별 글 추가 수집 필요(별도 약관 확인). 키워드 기반이라 동명 매장 오탐 가능. **주의**: developers.naver.com 검색 API는 폐지되고 NAVER API HUB(NCP)로 완전히 이전됨 — 인증 헤더도 `X-Naver-Client-Id` 대신 `X-NCP-APIGW-API-KEY-ID`/`X-NCP-APIGW-API-KEY` 사용. `link` 필드는 실제로 API가 항상 제공함(과거 "개별 글 링크 미제공"이라고 기록했던 건 구글 리뷰와 혼동한 오류였음) |

**"광고성 리뷰 필터링"의 현재 상태**: Google 리뷰는 실제 방문자 리뷰라 애초에 협찬/광고 판별 대상이 아니므로 `is_ad_filtered`는 전부 `false`로 저장됨. 네이버 리뷰는 스니펫의 협찬 키워드 매칭으로 1차 필터링됨(원래 계획한 LLM 기반 정밀 분류는 미착수 — 본문 전체 크롤링이 필요해 범위 밖으로 유보). "불친절 후기 제외"라는 인접 기능도 구현되어 있음 — `flag-rude-reviews` Edge Function이 리뷰 중 별점 2점 이하 + 부정적 키워드(불친절/무례/짜증 등)가 있으면 그 가게 전체를 검색 결과에서 제외(`has_rude_review`).

**리뷰 카드 클릭 동작**: `ReviewCard.jsx`는 구글 리뷰든 네이버 리뷰든 카드 전체가 클릭 가능한 링크임 — 구글은 `googlePlaceId` 기반 구글맵 페이지로, 네이버는 위 `link` 컬럼(블로그 원문 URL)로 각각 새 탭 이동. `taste_snippet`은 2026-08-18 현재 DB에만 채워져 있고 프론트 UI에는 아직 노출하지 않음(노출 여부는 별도 논의 필요).

### 10.2 현지인 비율 판별
Google Places API는 리뷰어 국적/언어 데이터를 제공하지 않아 아직 미구현 — `restaurants.local_ratio`는 현재 전부 `null`(프론트에서는 `0`으로 기본 표시). 판별 방법은 여전히 미정.

### 10.3 실시간 근처 백업 플랜 추천 및 영업중 판정
✅ **구현 완료** — `fetchBackupPlan`(`src/lib/restaurants.js`)이 같은 지역 + 같은 음식종류 후보 중 위경도 기준 하버사인 거리로 가장 가까운 곳을 실제로 추천함(실시간 위치가 아니라 원래 가게의 좌표 기준).

**영업중 뱃지는 2026-08-19부터 진짜 실시간 계산으로 전환됨.** 기존에는 Google Places의 `businessStatus`(OPERATIONAL/CLOSED_TEMPORARILY/CLOSED_PERMANENTLY, 폐업 여부만 구분)만 반영해 사실상 거의 모든 가게가 "영업중"으로 고정 표시되는 문제가 있었음. `sync-restaurants`가 `regularOpeningHours`(요일별 영업시간)를 추가로 요청해 `opening_hours`(jsonb) 컬럼에 저장하고, 프론트의 `src/utils/businessHours.js`(`isOpenNow`)가 일본 현지 시각 기준으로 지금 문을 열었는지 계산함. 폐업(`status='closed'`)과 "오늘 영업시간이 지나 문 닫음"을 구분해 각각 "휴무"/"영업종료"로 다르게 표시(`resolveStatusKey`). `opening_hours`는 Google 자체에 시간 정보가 없는 가게(전체의 약 34%)는 비어있어, 이 경우 기존처럼 `businessStatus` 기준으로만 폴백.

같은 방식으로 2026-08-18에 **"근처 디저트 맛집"** 기능도 추가됨 — `fetchNearbyDessert`가 음식종류 조건 없이 같은 지역의 `category='디저트'` 후보 중 가장 가까운 곳을 추천. 이 기능을 위해 디저트 카테고리 데이터를 14개 지역 전체에 신규 수집(224곳, 네이버 리뷰 포함).

### 10.4 역까지 도보 거리
PRD 원안에는 없었으나 실제 구현에서 추가된 기능. `fill-walk-minutes` Edge Function이 Places Nearby Search로 가게 좌표 기준 가장 가까운 기차역을 찾아 직선거리를 도보 분으로 환산(`walk_minutes`)하고 역 이름(`nearest_station`)도 저장함. 오키나와는 본토와 철도로 연결되지 않아 이 필드를 항상 비워둠(관광객 대부분 렌터카 이동).

### 10.5 핫페퍼 그루메 API 보강
✅ **구현 완료**(2026-08-18) — `enrich-hotpepper` Edge Function이 리쿠르트 Hot Pepper Gourmet API(グルメサーチAPI)로 Google Places가 못 채우는 영업시간(`business_hours`)·정휴일(`regular_holiday`)·예산대(`budget_dinner`/`budget_lunch`)·핫페퍼 URL(`hotpepper_url`)을 보강. `accepts_card`는 Hot Pepper가 명시적으로 "가능"이라 할 때만 덮어씀(기존 Google 값을 약한 신호로 잘못 덮어쓰지 않기 위함).

**매칭 방식이 원래 계획(가게명 매칭)에서 위경도 거리 매칭으로 변경됨**: DB에 저장된 가게명은 수집 시 자동 번역되어 한글/영문/일본어가 섞여 있는데, Hot Pepper API는 일본어 원문 이름만 반환함 — 실제로 동일한 가게인데도 문자 유사도가 0으로 나오는 걸 확인하고 전략을 변경. 대신 원래 위경도가 이미 Google Places 기준으로 정확하므로, Hot Pepper 검색 결과 중 반경 80m 이내 최근접 후보를 같은 가게로 신뢰. 전체 1,970개 가게 실행 결과 1,589곳 매칭(80.7%) — 나머지는 반경 안에 Hot Pepper 미등록이거나 실제로 없는 경우.

**UI 노출 완료**(2026-08-18) — `RestaurantDetailPage.jsx`의 주소·전화번호 블록 바로 아래에 영업시간(정휴일 포함)·예산대(디너/런치)·핫페퍼 그루메 링크를 추가로 표시. 값이 있는 필드만 조건부 렌더링(매칭 안 된 가게는 항목 자체가 안 보임). 영업시간·정휴일·예산 텍스트는 Hot Pepper가 일본어 원문만 줘서 처음엔 번역 없이 그대로 노출했으나, 사용자 피드백으로 Cloud Translation API 번역을 추가해 전체 재실행함(같은 날).

**신규 카테고리 보강(2026-08-20)** — 2026-08-19에 추가한 13개 카테고리(이자카야 등, 총 1,794곳)도 같은 방식으로 보강 실행, 1,567곳 매칭(87.4%). 이번엔 `enrich-hotpepper`에 `skipTranslate` 플래그(payload에 `skipTranslate: true`)를 새로 추가해 Cloud Translation API 호출을 건너뛰고 Hot Pepper 원문(일본어)을 그대로 저장함(GCP 크레딧 절약 목적) — 이 대상 가게들의 영업시간·정휴일·예산 필드는 아직 한글 번역이 안 되어 있으니, 나중에 크레딧 여유가 생기면 번역본으로 별도 재실행할 것.

---

## 11. 인증

- Supabase Auth 기반 **이메일/비밀번호 로그인**을 실제로 구현(`@supabase/supabase-js` 연동, 가입 시 트리거로 `profiles` 행 자동 생성). 비밀번호 변경도 마이페이지에서 현재 비밀번호 재인증 후 실제로 처리됨
- **구글 소셜 로그인 실제 연동 완료**(2026-08-19) — Supabase Auth의 Google OAuth Provider 사용, 로그인 화면에 "Google로 로그인" 버튼 추가. `handle_new_user` 트리거가 provider와 무관하게 `auth.users` insert마다 동작해 Google 신규 가입자도 `profiles` 행이 자동 생성됨
- 비로그인 상태에서 저장(⭐)·마이페이지 접근 시 `ProtectedRoute` 컴포넌트로 로그인 화면으로 분기, 로그인 성공 후 원래 목적지로 복귀
- 회원가입도 Supabase Auth로 실제 구현됨(이메일/비밀번호 입력 → 계정 생성) — 이 프로젝트는 이메일 확인이 꺼져 있어 가입 즉시 로그인 처리

---

## 12. 수익모델

현재는 수익화를 진행할 시점이 아니라고 판단해 **상세 확정(수수료율·가격)은 보류**하고 메모만 남긴다 (1단계 워크시트 기록).

| 옵션 | 구조 | 비고 |
|---|---|---|
| A. 예약 중개 수수료 | 제휴 식당 예약 성사 시 건당 수수료 | 타베로그·트리플과 동일 구조, **MVP 1순위** |
| B. 프리미엄 구독 | 여행 단위 결제로 필터링 결과 무제한 열람 + 백업 플랜 우선 노출 | 짧은 여행 기간 내 전환 위험, 사용자 확보 후 검토 |
| C. 현지 식당 제휴 광고 | 검증 배지·상단 노출권 유상 제공 | "광고 없는 찐맛집" 컨셉과 상충 위험 — 최후순위 |

---

## 13. KPI / 성공지표

수익화 이전 단계이므로 매출 KPI 대신 **사용성 검증 KPI**를 우선한다.
1. 가입자 수 (1년 차 10만 명 목표, SAM 수치 연동)
2. 여행 기간 내 재방문율 (재설치·재실행)
3. 핵심 기능(리뷰 필터링·백업 플랜 CTA) 사용률

수익모델 확정 후 예약 전환율 등 매출 KPI를 추가할 예정.

---

## 14. 법률/규제 검토 (별도 자문 필요 — 법적 확정 아님)

- **위치기반서비스**: 위치정보 자체 수집·가공 시 방송통신위원회 위치정보사업 신고 대상 여부 확인 (단순 지도 표시는 예외 소지)
- **리뷰 스크래핑**: 구글맵·타베로그 등 플랫폼 이용약관상 상업적 크롤링 금지 조항 확인, 공식 API 사용 우선(네이버·구글 API로 대부분 회피)
- **예약 중개**: 통신판매중개업 신고(전자상거래법) 및 PG(결제대행) 계약 필요 여부 확인

---

## 15. 일정/마일스톤 (1인 개발 기준 추정치)

1. PRD.md · CLAUDE.md 작성 — 1주
2. 핵심 화면(홈·검색결과·상세) 개발 및 Must-have 3개 기능 구현 — 3~4주
3. 리뷰 필터링·백업 플랜 기술검증(POC), 개발과 병행 — 1~2주
4. 내부 QA 및 소규모 사용자 테스트 — 1주
5. 스토어 출시 및 1차 KPI 측정 — 출시 후 1개월

---

## 16. 완료 기준 체크리스트 (Definition of Done)

- [x] 홈 → 검색 결과 → 맛집 상세까지 Must-have 기능 3개가 모두 3탭 이내로 도달 가능
- [x] 검색 결과 필터(평점·현지인비율·영업상태 + 실용정보 3종 + 음식종류) 정상 동작 — 단, 지역 필터는 설계 변경으로 삭제됨(검색창으로 대체)
- [x] 맛집 상세에 주소·전화번호·⭐ 저장 버튼·영업 상태 배지 노출
- [x] 네이버 블로그 리뷰(협찬 필터링 배지 포함) + 구글 리뷰(Google 출처 표기 포함) 병행 노출 — 2026-08-18 네이버 연동 완료(전체 1,748곳 수집, 1,470곳/6,598건)
- [x] 휴무/재료소진 상태일 때만 지도 + 백업 플랜 CTA 노출 — 단, 위치 권한 요청은 아직 없음(백업 플랜이 사용자 실시간 위치가 아니라 원래 가게 좌표 기준으로 동작해서 권한 자체가 불필요해짐)
- [x] 이메일/비밀번호 로그인(Supabase Auth) 정상 동작, 비로그인 시 보호된 화면(마이페이지) 접근 시 로그인으로 분기 후 원래 화면 복귀 — 실제 Supabase Auth 연동 완료(회원가입·로그인·로그아웃·비밀번호 변경)
- [x] 스크랩 목록에서 추가/삭제 정상 동작 (`scraps`) — 2026-08-19 실제 DB 연동 완료(로그인 시 계정별 동기화, 비로그인 시만 localStorage 폴백)
- [x] Supabase RLS로 본인 데이터만 조회/수정 가능한지 확인 — `profiles`/`scraps`/`search_history`는 본인만, `restaurants`/`reviews_cache`는 의도적으로 전체 공개 조회 허용
- [x] 외부 API(구글·네이버) 키는 Supabase Edge Function의 secret으로만 저장, 클라이언트에 노출되지 않음 — 단 Flask가 아니라 Edge Function으로 구현됨(9절 참고). 네이버는 `NAVER_CLIENT_ID`/`NAVER_CLIENT_SECRET` secret으로 등록됨

---

## 17. 제외 범위 (Out of Scope)

- 여행 일정 전체 관리(숙소·교통 포함 종합 플래너)
- 회원 등록/관리자 화면, 1:1 채팅, 리뷰 수동 정정 (MVP 이후 검토)
- 알림 기능 (마이페이지 UI는 존재하나 "준비중" 상태로 비활성화, 실제 발송 로직 미구현)
- 현지 식당 사장님 대상 제휴/쿠폰 등록 기능 (Could — MVP 이후)
- 수익모델 상세 확정(수수료율·가격) — 1단계 메모 참고, 별도 시점에 진행
